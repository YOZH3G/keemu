# Agent decisions

## D-001 — Reuse the existing repository

The durable supervisor uses `/opt/data/workspace/keemu` directly. `/opt/data/hermes-projects/keemu` is a compatibility symlink, not a second checkout. This avoids splitting project state.

## D-002 — Fixed current-model route

Supervised work is fixed to tier C5: `gpt-5.6-sol` with reasoning `xhigh`. Router mode is `auto` only to provide explicit per-run overrides and model-lock attestation; both minimum and maximum tiers are C5 and Astra is disabled. Dynamic model switching is not enabled.

## D-007 — ChatGPT 6 Luna/Sol policy refresh

At the verified p0-02 boundary, future Luna/Sol routes move from `gpt-5.6-luna`/`gpt-5.6-sol` to `gpt-6-luna`/`gpt-6-sol`. Existing attested route and outcome records retain the literal models that actually ran. The active p0-03 lock remains `gpt-5.6-terra` / `high`; all subsequent frozen plan entries and live policy/config use the updated IDs. Runtime attestation remains mandatory before a new model is treated as active.

## D-003 — Push each verified subtask through an isolated deploy key

The private remote is `git@github.com:YOZH3G/keemu.git`. Repository-local `core.sshCommand` uses only `/opt/data/.ssh/id_ed25519_keemu` with `-F /dev/null`, `IdentitiesOnly=yes`, and strict host-key checking, so the unrelated `hermes-workspace` key cannot create a false-positive access test. `git_auto_pull` remains disabled; `git_auto_push` is enabled and checkpoint mode is `all`, so each exact, attested subtask boundary commits new implementation files plus durable state and pushes `agent/keemu`. Secret-like paths remain refused by the supervisor checkpoint filter. No force-push or automatic merge to `main` is allowed.

## D-004 — Preserve the Hostinger baseline

The deployment reuses the installed Hermes binary, OAuth state, RTK, Graphify, Caveman and router runtime. It does not reinstall or reconfigure those components.

## D-005 — Direct QEMU/PRoot is diagnostic only

When Docker is unavailable, project-local QEMU and unprivileged PRoot may verify locked target artifacts, target shell/opkg, nested target ELF and shebang behavior. This path is not a replacement for the accepted Docker runtime and cannot satisfy container, binfmt, publishing, persistence, isolation or networking acceptance requirements.

## D-006 — Reports are frozen objects and write-once bundles

The current common report schema is version 2 and uses frozen nested metadata and immutable sequences. Aggregate status and coverage are derived and revalidated from checks and partial-failure state. Each partial failure references an exact operation-log sequence, and the referenced record must match its operation name and failure status. A report bundle is assembled in a sibling temporary directory and published with Linux `renameat2(..., RENAME_NOREPLACE)`. Publication atomically succeeds only when the destination entry is absent; any existing directory, file, or symlink is preserved and reported as `FileExistsError`. If the no-replace primitive is unavailable, publication fails closed and removes the temporary bundle rather than falling back to overwrite-capable rename. This keeps JSON, Markdown, and operation-log evidence aligned and preserves failed runs.

## D-008 — P0 fixtures use a locked host cross-toolchain

P0 fixture compilation uses a project-staged Debian trixie `aarch64-linux-gnu-gcc` toolchain whose exact DEB inputs and SHA-256 values are recorded in `locks/p0-fixtures-aarch64.json`. This creates reproducible AArch64 ELF sources without pretending that the host compiler or PRoot diagnostic is the required Docker/binfmt runtime. The raw NFQUEUE fixture uses locked Linux UAPI headers rather than an unpinned library SDK; kernel capability and packet-flow correctness remain later, separately bounded experiments.

## D-009 — Mixed-image architecture and deferred container limits

P0-04 uses a scratch `linux/amd64` image with a project-owned, statically linked amd64 PID 1 in `/__keemu` and locked AArch64 Entware application files elsewhere. The native binary is outside application PATH; labels separate the native platform from the target. Docker image ID and saved archive config digest are recorded distinctly because the Engine export produced different values. Resource limits and per-run ownership are represented in an argv-only Docker create template, not falsely attributed to immutable OCI image properties. Image build/inspect and saved-layer ELF audit are verified; runtime execution, applied limits, recovery and isolation await later subtasks. See `docs/decisions/0002-p0-mixed-image.md`.

## D-010 — Derived P0-05 image fixes inherited keeper signal dispositions

The original P0-04 init's keeper inherited handled SIGTERM/SIGINT and remained alive after a direct signal, so the old image could not prove forwarding. Preserve the original image, source, lock and audit. Derive a separately locked P0-05 image by replacing only the static amd64 init: the keeper restores default stop-signal dispositions; PID 1 returns success only after the forwarded signal is observed as the keeper's exact signaled exit. A real Docker/binfmt differential verified unexpected keeper death exits 1, while SIGINT to PID 1 exits 0; a full target/runtime probe verified nested execution, reaping and clean SIGTERM stop. No target app files, host binfmt entry, network, public port or foreign container was modified in the derived-image probe. See `docs/decisions/0003-p0-init-signal.md`.

## D-011 — Live quota admission before every frozen subtask

KEEMU uses `quota_admission_scope=every_subtask`. Before each new frozen subtask receives a worker launch, the supervisor fetches live OAuth account usage and persists an admission key containing the subtask ID and digest. A supervisor restart may reuse that same subtask observation before launch, but advancing the active index forces a new observation. Existing same-subtask sessions resume without another preflight. Remaining quota below 11% warns; below 5% stores an absolute wake time, checkpoints and waits. Usage-observer failure remains fail-open without inventing a percentage or reset time.

## D-012 — P0-06 localhost proof uses a constrained Docker-host observer

The Hermes worker network namespace cannot directly observe Docker daemon host loopback, so P0-06 retains explicit `127.0.0.1` TCP/UDP publishing on the AArch64 application container and uses one separate, project-owned native amd64 scratch observer only under the user-approved `--network=host` scope. The observer has no host mutation path, bind mounts, published ports, privilege, writable root, or retained runtime state; it has owner/run-id labels, `cap-drop=ALL`, no-new-privileges and bounded memory/CPU/PID limits. It independently reads the published HTTP health endpoint and UDP echo, records Docker inspect/log evidence, and is owner-checked/removed/absence-verified after each observation. This validates Docker-host loopback reachability without widening the application publish or authorizing later work.

## D-013 — P0-07 NFQUEUE blocker is not a verdict substitution

In project-owned isolation, the native NFNETLINK socket opens but the statically linked AArch64 socket returns `EPROTONOSUPPORT`. The original dynamically linked target consumer also fails the Entware libc version check; static target execution alone does not establish queue support. Host NFQUEUE modules were not active and host-module mutation requires separate approval. No queue binding or rule was attempted. This is an explicit bounded P0 blocker, not A13/A14/A17 PASS; MVP 1C stays blocked. `docs/decisions/0004-p0-nfqueue-gate.md` has the evidence and scope.

## D-014 — P0 closure distinguishes gate completion from acceptance completion

P0 is closed when its `TASK.md` technical-risk conditions have exact evidence: real AArch64 execution, explicit localhost publish plus down/up persistence, and either NFQUEUE ACCEPT/DROP evidence or a reproducible capability blocker, with committed locks and ADRs. P0-07 supplied the latter blocker; it does not make A13/A14/A17 PASS. `docs/evidence/p0-evidence-manifest.md` records recomputed SHA-256 digests for all committed P0 locks and retained ignored runtime reports. ADR-0005 limits the P0-06 Docker-host observer to localhost-evidence collection; it is not a general host-network component or later network-topology authorization.

## D-015 — MVP 1A inputs are versioned and do not confer runtime authority

Scenario, production scenario lock, and persistent-environment inputs use strict version-1 Pydantic models with committed JSON-schema parity. Historical P0 locks retain their original formats; no migration or rewrite is implied. Unknown fields/versions, duplicate YAML/JSON keys and aliases fail closed. Scenario-relative file references may use `..` only when normalized inside the project root and all checked path components are non-symlinks. Target persistence/cleanup paths are canonical absolute `/opt` descendants; host publishing is localhost-only by default and in this schema cannot request public exposure. Lock loading verifies local source bytes and, when bound to a scenario path, scenario bytes against SHA-256, but profile/feed/OCI/QEMU facts and real Docker ownership labels still require later runtime evidence. Loader checks alone do not close validation-to-use races; later consumers need no-follow file opens and rechecks. The persistent-environment model is read-only here: registry writes, locking, reconciliation and cleanup are separate frozen subtasks.

## D-016 — Static IPK inspection is bounded input triage, not installation proof

The m1a-10 inspector reads each IPK with a regular-file/no-follow open, 64 MiB compressed-input cap, 32 MiB member cap, 128 MiB expanded-content cap and 8192-entry cap. It detects the actual selected feed's gzip-tar outer wrapper and supported ar/plain-tar/xz/bzip2 variants from signatures, requires unique `debian-binary`/control/data members, validates tar end markers and never writes archive content to a host tree. It rejects special members, unsafe/duplicate paths, link escapes, symlink-parent traversal, cycles and malformed control/ELF metadata. The bounded ELF parser reads PT_INTERP, DT_NEEDED and limited loader search paths without host `ldd`; optional rootfs lookups never intentionally follow links outside the target `/opt` tree. A rootfs must include resolved dependencies before missing `/opt` loaders/libraries can be package FAIL; absent/non-`/opt` paths and postinst-created links are BLOCKED or WARN rather than false pre-install FAIL. World-writable regular entries fail; setuid/setgid and shebang files not known to be invoked are reported for review rather than assumed fatal. Static PASS only certifies this inspection pass. Install-time input reopening/rehashing, opkg state, exactly-once postinst and target execution remain later subtasks; no A02 acceptance is inferred.

## D-017 — Locked AArch64 base init uses an immutable, audited image cache

For m1a-11, only the existing 20-package P0 AArch64 Entware closure, SHA-256-pinned feed index/bootstrap/QEMU inputs and separately locked corrected native init are accepted. `keemu init --profile generic-aarch64 --locked` invokes real target opkg on local IPKs in a staging rootfs, requires exact installed package names and versions, normalizes only opkg's volatile `Installed-Time` fields, and writes a default target `/opt/etc/opkg.conf`. The origin URL is HTTPS but not an immutable upstream snapshot; init never runs `opkg update`, and later installation must use locally hash-verified IPKs. The static amd64 native init binary is rebuilt and matched to the P0-05 binary SHA-256. The saved single-layer scratch image is audited against every rootfs file/link/mode; Docker COPY strips exactly one setuid bit on `/opt/bin/busybox`, and no other difference is accepted. The committed `locks/m1a-init-aarch64.json` binds the schema-4 cache key, rootfs tree, installed inventory, feed config/index, QEMU binary, saved archive/config/layer and Docker-local image ID (not a registry manifest digest). Build/cache publication uses an interprocess file lock and `renameat2(RENAME_NOREPLACE)` after a bounded, labeled Docker/binfmt smoke; an existing incomplete or divergent cache fails closed. Offline repeat rehashes prepared artifacts and inspects the live image without fetch/build; it requires the local image to remain present. Target shell, default opkg inventory, nested ELF and DNS passed in Docker bridge. TLS was verified only in the Hermes process namespace with default CA/hostname validation because locked target BusyBox wget cannot do HTTPS; this is not target HTTPS evidence. No application fixture install, scenario lifecycle, registry mutation, host configuration or cross-target acceptance is inferred. Two independent builds matched all pinned rootfs/image/archive hashes; SHA-256-bound ignored evidence is in `reports/20260923T202746Z-m1a11-base-init/init-evidence.json`.

## D-018 — Docker boundary is ID-verified and reconciliation is read-only

m1a-12 uses an immutable Docker-local image ID, inspected project/target/native-init labels and a new writable overlay per project/run-labeled container. Fixed argv-only Docker calls impose CPU/RAM/PID/tmpfs/log-driver bounds and prohibit mounts, privileged/host namespaces and added caps; bridge networks are independently owned resources. Exact full-ID inspect with owner, run ID, kind, base and target labels precedes mutation; network removal refuses attachments. Reconciliation enumerates and individually inspects labeled IDs but never auto-deletes unexpected resources or adopts missing resources. A 512 MiB observed writable-layer check can stop an owned container at operation boundaries, but is not a hard/continuous disk quota; Docker log-driver rotation is not detectable by the per-stream retrieval truncation marker. Full registry reconciliation, interruption recovery, host disk budgeting and lifecycle policy remain later tasks. See `docs/decisions/0006-m1a-docker-boundary.md` for live evidence and limitations.

## D-019 — One-shot lifecycle preserves primary failure and scope

m1a-13 accepts only a locked, disposable AArch64 IPK test. Reopen/no-follow/rehash scenario, lock, profile, source and base; inspect the exact private IPK bytes, offline-verify the image, hash-read back the target copy, then use real target opkg. Observe inventory, explicit checks, target-loopback HTTP readiness and refused connection after stop, remove, and compare only Docker path/change metadata against baseline and explicit allowances. The immutable base lacks a gzip command link used by opkg; create a checked target BusyBox gzip link only in the disposable container, before the diff baseline, and record the substitution. Run-ID collision is not cleanup authorization. Every normal failure attempts only owner-checked cleanup; the primary failed operation remains referenced even if cleanup separately fails and raises overall ERROR. Write-once reports retain bounded command evidence and validated input bytes. No persistent environment, host publish/HTTPS/UDP, SIGKILL recovery, byte-complete residual comparison or full A02–A06/A18/A21 acceptance is inferred. See `docs/decisions/0007-m1a-one-shot-lifecycle.md`.

## D-020 — Persistent registry rejects name and identity replacement

m1a-14 uses owner-only per-name flock files and one no-replace directory per persistent name under ignored `.runtime/registry`. State JSON is published with fsynced temporary files, atomic replacement and locked byte/inode compare; a digest-bound scenario snapshot is immutable. Destroy retains a tombstone. Preflight reopens and hashes source/profile/scenario/lock bytes and verifies the immutable base; Docker labels, image and exact full IDs are re-inspected independently of registry before lifecycle mutation. Real target IPK installation/readiness is scoped to AArch64 with no publish, extra capabilities, NDM, secret environment or non-target-loopback HTTP service. Normal create failure retains a failed record and attempts only owner-verified cleanup; start/stop failure retains failed state and blocks further mutation pending safe recovery, without automatically deleting persistent state. Persistent operations do not yet publish the one-shot report bundle; interrupted create, cleanup retry and full A03–A06/A18 acceptance are later tasks. See `docs/decisions/0008-m1a-persistent-registry.md`.

## D-021 — m1a-15 TLS fixture is not generic lifecycle authority

A separately SHA-256-locked static AArch64 TLS frontend serves the existing P0 web-demo over HTTPS; local CA/server keys are generated in ignored owner-only runtime state and removed after the live probe, never added to host or target-wide trust. A disposable, labeled, read-only, cap-drop-all host-vantage observer probes only explicit `127.0.0.1` Docker publishes and is owner-checked absent after each observation. The test rereads ephemeral published ports after same-container Docker restart rather than assuming their stability. Clean offline SDK rebuild, target HTTPS with CA/hostname validation, negative trust/hostname tests, HTTP/UDP, persisted HTTPS state and twice-repeated offline one-shot hello all passed. This isolated evidence fixture does not relax generic `keemu test` or persistent `up` constraints; full acceptance remains PARTIAL. See `docs/decisions/0009-m1a15-aarch64-fixtures.md` and `docs/evidence/m1a15-evidence-manifest.md`.

## D-022 — Quota classification clears iteration-local completion state

The durable supervisor must initialize the active-subtask completion flag on every worker-result iteration, before quota handling. A quota response after an earlier exact completion must never reuse the previous iteration's terminal value or enter the subtask-boundary checkpoint path. The regression runs one exact completion followed by a 429 and requires one real boundary, one quota wait, unchanged successor `active_index`, and preserved successor session/lock. Historical journal entries 241–372 saying “Verified subtask boundary” are incident artifacts from this defect, not additional completed subtasks; durable state remained at active `m1a-16` throughout.

## D-023 — Recovery discards uncertainty only after exact ownership proof

For m1a-16, persistent `recover --yes` is an explicit, flock-serialized cleanup path, not an automatic restart or postinst replay. A pre-ID orphan is eligible only when exactly one container has the recorded run/base/target/owner labels and the deterministic `keemu-<run_id>` name. A recorded ID that still exists but no longer passes the owner-filtered query is refused; extra owned containers/networks and healthy environments are refused. A removal error leaves the transitional state for an explicit retry. Owner-checked absence precedes failed-to-destroyed tombstoning. Real SIGKILL at two create boundaries plus cleanup injection passed on the Docker runner, but one-shot crash journaling, interrupted partial reports, automatic recovery, hard disk limits and full socket census are not proved. A18/A20 remain PARTIAL; see `docs/decisions/0010-m1a16-recovery-isolation.md`.

## D-024 — Freeze scoped acceptance without promoting a blocked gate

m1a-17 uses an immutable, hash-bound JSON ledger generated from counted JUnit cases, selected exact live reports, runner inspection and owner-label absence. Every A01–A09/A18–A21 whole ID remains BLOCKED when a mandatory facet is unproved; a passing AArch64 slice never upgrades it. Expected negative package reports retain their own FAIL while their verifier tests PASS. MIPS/MIPSEL and clean-Ubuntu validation are explicit SKIP, not simulated by the shared Docker/binfmt host. The latter had no KEEMU-owned resources and preserved foreign before/after state, but is not a fresh VM. This closes the bounded acceptance-observation subtask, not MVP 1A or release acceptance. The ledger refuses overwrite; ignored raw evidence is retained locally by digest.

## D-025 — MIPS target artifacts are locked; host binfmt remains an approval boundary

m1b-18 maps the specified `mipsel-3.4`/`mips-3.4` Entware architecture IDs to the observed public feed directories `mipselsf-k3.4`/`mipssf-k3.4`, captures the feed index and each exact package/bootstrap byte with SHA-256, and installs a 20-package root through the corresponding real target bootstrap opkg. A separate 16-package SDK closure builds project-owned hello/web-demo/NFQUEUE fixtures with Entware target GCC; using Debian hard-float cross libc for these soft-float targets failed the `gnu/stubs-o32_soft.h` prerequisite, so it is not substituted as a compatible SDK. An existing pinned native amd64 init plus audited MIPS application files produces local scratch images; saved layers are compared file-for-file with the staged roots, allowing only the observed BusyBox setuid attenuation. ELF32 endian, o32, MIPS32r2 and soft-FPU attributes and exact installed inventory are verified. Direct QEMU/PRoot shell/opkg/nested/shebang/fixture PASS is diagnostic only. Project-owned nonprivileged Docker target exec failed `exec format error` on both while native init ran; no usable host MIPS binfmt was proven and no registration was authorized or changed. A01 three-target acceptance and m1b-18 completion remain BLOCKED. A10 matrix belongs to m1b-19 and is NOT RUN. The frozen `docs/evidence/m1b18-targets.json` binds this blocker and retained ignored raw probe; a future approved capability correction must retain, not rewrite, the historical blocker. No host module, firewall, public port, privilege or foreign Docker resource was changed.

## D-026 — Approved MIPS binfmt uses exact pinned interpreters and fail-closed readback

After explicit approval, add only `qemu-mips` and `qemu-mipsel` using the existing pinned Debian qemu-user bytes (`7ccb9c…` and `40be2a…`) and Debian machine-specific registration templates. Refuse pre-existing same-name handlers and preserve `qemu-aarch64`. Require readback of enabled state, full magic/mask, interpreter and POF flags before Docker execution. A first attempt decoded template NUL escapes before writing and produced dangerously truncated seven-byte magic; it was immediately removed and the baseline was independently restored before corrected literal-escape registration. The corrected handlers passed both exact-target Docker probes and left no project-owned resource. Backups and readback are under `/opt/data/backups/keemu-stack-20260922T073131Z/binfmt-mips-approval-20260924T192130Z/`. This approval does not cover other handlers, host modules, firewall, public exposure or foreign resources.

## D-027 — Freeze approved MIPS Docker PASS separately; preserve historical BLOCKED ledger

The separately approved host binfmt correction changes capability, not prior evidence: do not modify the initial `m1b18-targets.json` BLOCKED ledger or m1a-17 AArch64 ledger. The approval-time probe reused the old ignored `.runtime/m1b18/probe.json` path, overwriting original BLOCKED raw bytes; retain the historical digest and say the raw is unavailable rather than fabricating a replacement. Future bounded probes write unique paths. A new no-replace `m1b18-targets-pass.json` binds tracked raw PASS bytes, both lock sets, current offline integrity, prior AArch64 PASS, exact direct+Docker target markers and owner-clean resource queries. A01's specified generic target ELF/shell/nested-exec check can now be marked PASS across all three targets, but the general MIPS `keemu init`/`test` lifecycle, A10 matrix and MVP 1B are not inferred from the target probes. Do not promote NDM, event or network claims.

## D-028 — Matrix completeness and unsupported target are required BLOCKED

Use an explicit scenario lock for every matrix case because the runnable one-shot lifecycle requires it; reject ambiguous/unsafe/duplicate inputs before Docker. Require all three generic profiles for complete A10 coverage, with missing cases BLOCKED, not a smaller PASS. A verified wrong-architecture IPK is package FAIL even when other cases are BLOCKED; unsupported MIPS/MIPSEL general lifecycle remains environment BLOCKED despite their separate target-exec probes. Visit declared cases sequentially; preserve each executed child's independent immutable report and bind path/hash into a parent schema-version-2 report. Input/schema errors exit 2 without a report bundle (A21 gap). No MIPS lifecycle/NDM/event capability is inferred. See `docs/decisions/0011-m1b19-matrix.md` and `docs/evidence/m1b19-matrix.md`.

## D-029 — NDM shim is an exact local process contract, not device emulation

The m1b-20 strict version-1 contract enumerates exact `ndmc -c` argv and process response bytes/source; unknown argv exits 64 with `unsupported-ndm-command`, and a required-result adapter reports BLOCKED/environment-gap instead of blaming the package. Writes change durable modeled state, reads distinguish all declared values, and repeated no-effect writes exit 65. Local flock, atomic fsynced state and digest-bound contract refusal preserve cooperative process semantics; JSONL logs redact command text and exclude arbitrary environment and output. Portable synthetic fixture subprocess tests establish only this host-side shim slice: no physical observed sample, target binary installation, package lifecycle integration or event contract. A11/MVP 1B are not PASS. See ADR-0012.

## D-030 — Synthetic event order and repeat do not prove kernel firewall restoration

For m1b-21, run only explicitly opted-in host-local `synthetic:` boot/netfilter contracts from owner-only test fixtures. Filename order, fixed directory/selector/cwd/PATH, SHA-256 per handler, explicit allowlisted event variables, per-handler timeout, nonexecutable and continuation policies, and exact rule-list before/after are enforced. Unknown events return BLOCKED without executing handlers; script failure or nonrestoration returns FAIL. Real Python subprocess tests restore deliberately changed test-owned JSON state on repeat without duplicate identifiers, but no observed device contract or actual firewall event/rule is proven. Do not add unverified event claims to generic profile YAML or wire this harness into persistent `keemu event`/`test`. A12 and MVP 1B remain BLOCKED. See ADR-0013 and `docs/evidence/m1b21-events.md`.

## D-031 — Three-target acceptance records blockers without executing unapproved network mutations

For m1b-22, freeze all currently runnable opt-in lifecycle/matrix/offline integrity tests and three per-target Docker capability differentials in no-replace, tracked, SHA-256-bound evidence. The existing A01 generic ELF/shell/nested-exec PASS is bounded; A02–A06/A10/A11 remain BLOCKED as whole IDs across all three targets. A complete matrix with one real AArch64 child PASS and two static MIPS/MIPSEL BLOCKED cases is an A10 BLOCKED result. The host NDM process shim is synthetic and does not stand in for installed target `ndmc` or physical observations. In owned, network-none, cap-drop-all containers, only target/native `NETLINK_NETFILTER` socket creation is attempted: each target returns `EPROTONOSUPPORT`, each native same-namespace control succeeds. No queue bind, iptables/ipset mutation, packet, verdict, module load, host network or public publish follows. Backend presence is not functionality. Worker `doctor` binfmt BLOCKED/4 is a namespace-visibility gap, not contradiction of separate Docker target execution. Keep historical m1a-17/m1b-18/m1b-19 ledgers immutable. See ADR-0014 and `docs/evidence/m1b22-acceptance.md`.

## D-032 — Isolated Docker bridges, netlink br0 and endpoint-named wan0

For m1c-23, provision distinct internal Docker LAN/WAN bridges with dynamically allocated, non-overlapping `/24` pools under a private project-local flock; Docker IPAM itself rejects concurrent overlap. Client joins LAN only, server WAN only, router both. No management network is attached and Docker exec by owner-verified full ID is the sole control path. Give only namespace-scoped `NET_ADMIN` to configure client/server routes and the router's real `br0`; Docker `--sysctl net.ipv4.ip_forward=1` sets namespace forwarding. Under QEMU, target `brctl` and interface rename ioctl failed; use target BusyBox netlink for bridge creation/enslavement and Docker endpoint `ifname=wan0` driver option instead. Require loaded bridge support before any attempt; do not load host modules or mutate the host firewall directly. Use explicit cleanup: re-inspect labels/full IDs/role names, refuse unexpected resources, remove containers before their networks, verify absence. Live HTTP/counters and concurrent-run/rollback tests passed; do not infer network-demo, NFQUEUE verdict or whole A13/A18/A20 acceptance. Docker-host non-Docker routes are not visible from this worker; allocator checks Docker IPAM and worker routes, and Engine may still refuse a colliding pool. See ADR-0015.

## D-033 — Network-demo reuses P0 queue code and refuses API-only verdict claims

m1c-24 compiles the unchanged SHA-256-locked P0 consumer source into a static AArch64 network-demo translation unit. API/UI controls a single atomic mode file and shows actual process counters and queue readiness; `--api-only` is an explicit diagnostic with `queue_ready=false` and `/health` 503, never a network acceptance mode. Target real queue startup requires queue-bind/copy ACKs before API readiness, and on the current Docker host returns `Protocol not supported` without a rule or verdict. The fixture-only Python hooks stage and target-hash-check the binary, keep mode across service stop, and require actual queue readiness before installing an exact client `/32` to server `/32` TCP:8080 FORWARD NFQUEUE rule, excluding router LAN control:9090 and forbidding queue-bypass. This hook is not yet an exercised firewall rule or generic `keemu up` scenario. Preserve the historical P0 lock/source and the separate m1c-25/m1c-26 acceptance boundaries: A15/A16 partial slices, A14/A17/MVP 1C BLOCKED. See ADR-0016 and `docs/evidence/m1c24-network-demo-raw.json`.

## D-034 — Separate API and persistence evidence without network promotion

m1c-25 freezes distinct raw A15 web/API and A16 service/environment-persistence records from one owner-checked target run. API-only permits direct verification of GET UI/state, valid and invalid POST and file readback without lying about queue readiness. The mode file survives service restart and same-container Docker stop/start, but this latter operation recreates the network namespace without re-establishing router `br0`; only Docker-provided LAN control reachability was asserted afterward. A15/A16 have scoped PASS slices yet remain PARTIAL as whole IDs: selected mode has not been applied to packet processing or a generic persistent CLI environment. No rule, packet, verdict, module or host-firewall change; m1c-26 remains the separate packet boundary. Tracked records and digests are in `docs/traceability.md`.

## D-035 — m1c-26 cannot promote a routed baseline to target packet verdicts

On this runner, the owned AArch64 topology forwarded a no-queue HTTP request (router `ForwDatagrams` 0→10), but the same router namespace's static target NFNETLINK socket returned `Protocol not supported` while the native control opened. Real target network-demo queue startup failed before binding/ACK or API readiness. With `nfnetlink_queue`, `xt_NFQUEUE` and `nft_queue` absent, installing a rule or binding a native queue could trigger host module autoload; neither is authorized. The diagnostic API-only rule hook refused installation. Freeze a BLOCKED raw record with explicit NOT RUN verdict/counter/pcap fields and owner-verified cleanup; do not call the baseline a packet-processing PASS. Resumption requires an operator-prepared capable host or explicit bounded module-preparation authorization, followed by a new live target queue/rule/verdict experiment. The native socket success alone neither proves kernel queue support nor isolates QEMU ABI versus unloaded module causality.

## D-036 — Approved NFQUEUE preparation loads one module without firewall mutation

The explicit bounded host NFQUEUE preparation approval permits loading only `nfnetlink_queue` from the running kernel's installed, SHA-256-recorded module through host `kmod`. It does not permit arbitrary modules, persistent host firewall rules, sysctl changes, public exposure or foreign-resource mutation. Require pre/post module and kernel-taint readback, zero KEEMU-owned residual helper resources, and a fresh target queue capability check before any exact owner-scoped packet rule. Preserve the prior m1c-26 BLOCKED record; capability preparation is not completion evidence.

## D-037 — Loaded handler is not sufficient for target queue readiness

The approved `nfnetlink_queue` load was followed by a fresh, owner-checked AArch64 router namespace probe. Native NFNETLINK socket creation succeeds there, but the locked static AArch64 socket and real network-demo still return `Protocol not supported`. The module inventory stays unchanged, and `xt_NFQUEUE`/`nft_queue` are not loaded. Do not install the queued-flow rule or autoload a different host module: the target consumer is not ready, and the sole host-module approval does not extend to firewall extensions. Freeze the second BLOCKED raw result separately from the historical no-module result. This differential does not prove whether QEMU syscall translation, seccomp, target ABI or another boundary is causal; a preconfigured compatible runner or an explicit reviewed workaround/host-module authorization is needed before a real target verdict attempt.

## D-038 — Native adapter is transport-only; AArch64 owns verdict decisions

The separately approved m1c-26 workaround may add one project-owned native x86_64 NFQUEUE transport adapter inside only the owned router network namespace. It may bind queue 42, transport bounded packet metadata/payload to the locked AArch64 network-demo over explicit bounded IPC, receive that target process's ACCEPT/DROP decision, and submit the corresponding kernel verdict. Target, adapter and kernel counters must correlate; the AArch64 process must select and count every verdict. This is a declared native transport substitution, not proof that direct AArch64 `NETLINK_NETFILTER` works. The exact `/opt/sbin/iptables` rule requires `xt_NFQUEUE`; its separately approved load is recorded by exact module hash. No `nft_queue` or other module, host-namespace/persistent rule, public exposure, QEMU/seccomp change or foreign-resource mutation is allowed. Require pcap, no-listener behavior, full owner cleanup and safe `xt_NFQUEUE` rollback when unused.

## D-039 — Native iptables-legacy is a pinned rule installer, not target compatibility

The separately approved firewall workaround may use one project-owned native x86_64 `iptables-legacy` backend only inside the owned router network namespace. It must be reproducibly sourced or staged, version/source/recipe/binary/dependency SHA-256 locked, placed outside target PATH and identity-checked before use. It may install and remove only the exact client `/32` → server `/32` TCP:8080 `NFQUEUE --queue-num 42` rule without queue-bypass. The AArch64 process remains the verdict decision owner through the D-038 IPC contract. Report the native firewall tool as an explicit substitution; do not claim target iptables compatibility. Nftables, ambient/unpinned tools, host-namespace/persistent rules, other modules, public exposure and foreign mutation remain forbidden. Require rule readback/counters, pcap, no-listener behavior, owner cleanup and safe `xt_NFQUEUE` rollback.

## D-040 — Pinning userspace does not authorize the legacy filter kernel module

The D-039 native backend image and Debian source/binary/dependency hashes are locked in `locks/m1c26-native-iptables.json`, but a fresh owner-router read-only `/proc/net/ip_tables_names` was empty. The running host has no loaded/built-in `iptable_filter` registration; an installed loadable `iptable_filter.ko.zst` exists. A first legacy `filter` table operation could request/autoload that host module. The D-039 authorization explicitly excludes other modules, so do not run even a table listing, check or insert as a speculative capability probe. Preserve `docs/evidence/m1c26-iptables-prerequisite-blocker.json` and request explicit bounded host-module authorization or a preconfigured runner. If separately approved, revalidate module hash, taint, use count, exact namespace/rule scope, and safe rollback; do not treat pinned userspace as packet verdict proof.

## D-041 — Approved filter preparation is one module and exact rollback order

The explicit bounded preparation authorizes only installed `iptable_filter` for the running kernel, after exact path/hash and taint preflight. The prior D-039 approval separately permits fresh-preflight reload of `xt_NFQUEUE`; no other module is inferred. Neither load authorizes a firewall command by itself. Before the first exact rule, require pinned backend identity, owner namespace, empty duplicate check and exact client/server `/32`, TCP:8080, queue-42, no-bypass argv. Cleanup must remove and read back the exact rule before resources, then unload `xt_NFQUEUE` and `iptable_filter` in that order only when use counts are zero. Preserve `nfnetlink_queue`; no nftables, host-namespace/persistent rule, public exposure or foreign mutation.

## D-042 — One-time quota-check bypass is limited to finishing m1c-26

The user explicitly directed completion of the active m1c-26 while omitting its next fresh quota check, then explicitly superseded the reset-bound schedule with immediate launch. Preserve the existing session `20260925_021217_b58cec` and runtime lock `openai-codex/gpt-6-sol/xhigh`; do not substitute a model or create a new worker session. The last real observation remains 0% with exact reset `2026-09-25T11:20:52+00:00`, so an immediate provider 429 is possible and must preserve the same task/lock. Remove scheduled job `e459a5e52c9d` before immediate launch to prevent duplicates. This is a one-time user-authorized exception for this active subtask only; every successor keeps mandatory fresh quota admission.

## D-043 — m1c-26 bounded packet proof uses a NET_RAW firewall helper; no direct target NFNETLINK claim

The owner-router native `iptables-legacy` helper with only `NET_ADMIN` returned permission denied even for a filter read; a same-namespace raw IPv4 socket probe failed without `NET_RAW` and passed with it. Add only `NET_RAW` to that disposable, owner-labeled firewall helper alongside `NET_ADMIN`, not to target packages or host namespace. Under the previously approved D-038/D-039 substitutions and explicitly authorized exact `iptable_filter`/`xt_NFQUEUE` module preparation, the bounded live run correlates target 10 ACCEPT/7 DROP, adapter 17 verdicts, kernel queue sequence 17, exact-rule counter 17 and pcap 17; routed HTTP accepted/dropped/restored, no-listener failed closed and the exact rule was removed. Keep the earlier direct target socket `Protocol not supported` and absent target iptables as distinct limitations; a stale target `queue_ready=true` after adapter death is not live-listener evidence. Safe rollback unloads `xt_NFQUEUE` then `iptable_filter` only with zero use counts; leave the previously approved `nfnetlink_queue` loaded. Raw evidence and independent postflight are in `docs/evidence/m1c26-packet-verdicts.json`, `docs/evidence/m1c26-packets.pcap`, `docs/evidence/m1c26-final-rollback.json`; frozen m1c-26 ends here, not a blanket MVP 1C PASS.

## D-044 — m1c-27 crash recovery is explicit; foreign policy rules are the rule boundary

To test network interruption without requesting new host module/firewall authorization, SIGKILL the exact `topology.create` process at three verified resource boundaries, then reconcile and remove only run-owned full IDs twice. A separate project-owned foreign run retains a real IPv4 routing-policy rule throughout; a pinned native Python observer joins only its internal client namespace for raw rtnetlink dump/add because target BusyBox `ip rule` fails under QEMU. Before and after every kill/recovery, exact foreign rule dumps and Docker identity/state snapshots must match; inspect all interrupted nodes for A20 isolation. No host namespace, host firewall table, `xt_NFQUEUE`/`iptable_filter` load, public port or target NFQUEUE rule is authorized by this decision. The three real SIGKILL cases and owner-clean readback passed; `docs/evidence/m1c27-network-interruption.json` SHA-256 `6d60b47c6e4b284b1d615e401f46579ad8f2fbd2f3e0ecf83c939a79e2a32537` retains the raw record. This is a scoped A18/A20 regression, not proof of foreign firewall-rule parity, automatic startup recovery, an A21 interrupted partial report, whole-ID PASS or MVP 1C completion; see ADR-0017.

## D-045 — Final acceptance ledger uses whole-ID release semantics

Freeze final-28 as a new hash-bound reconciliation rather than rewriting historical m1a-17, m1b-18, m1b-22 or network records. `docs/evidence/final28-acceptance.json` SHA-256 `7d3c0b10dc0c0436b18d4a028bb4e27b80d113fc734adac009784f13dfd74c4` binds all A01–A21 to retained evidence and test paths. The specified A01 three-generic-target execution check is PASS; the remaining whole release IDs are BLOCKED wherever a mandatory generic/target facet or per-run proof is absent. This does not erase PARTIAL or PASS at earlier bounded slice scope: A14/A17 have real substituted native-transport fixture PASS, but direct target NFNETLINK and generic lifecycle remain unproved. Negative defective-package reports remain FAIL while their harness tests PASS. Preserve the initial MIPS BLOCKED observation, its unavailable overwritten ignored raw bytes, the later approved target-exec PASS, and matrix BLOCKED chronology. `scripts/validate_final28.py` checks source hashes, historical JUnit counts, target statuses, packet/pcap correlation and interruption gaps without rerunning privileged tests. Final-29 release verification, final-30 documentation closure and final-31 independent audit are separate gates; neither this ledger nor portable tests make MVP 1 complete.

## D-046 — Release verification retains failed prerequisites and scoped skips

The frozen final-29 full sweep is a real FAIL (233 pass, 2 fail, 3 intentional skip), not a release PASS. Both failed cases require exact Docker-local historical image IDs that are now absent; the saved exports' config SHA-256 values differ from those IDs, so importing them cannot be assumed to restore the lock identity. Preserve original JUnit/failure logs and locks; do not replace images or rewrite failures into PASS. Run an independent portable-only check, read-only/offline target and lock verification, the approved owner-isolated network cases, and an exact clean-host readback, recording each distinct result in `docs/evidence/final29-release.json`. With `xt_NFQUEUE`/`iptable_filter` absent, do not rerun the privileged packet/rule job or load host modules merely to turn a SKIP green; retained m1c-26 substituted fixture evidence remains historical. Repository-wide formatter drift in six unchanged committed files is reported as FAIL, not silently edited in a verification-only subtask. Shared-host owner-clean evidence does not constitute a fresh Ubuntu VM. `scripts/validate_final29.py` rechecks retained hashes, JUnit statuses, probe counts and clean-host snapshot without new Docker mutation. Completion of the observation does not close the release gate, A02–A21 or the separate final-30/31 subtasks.
