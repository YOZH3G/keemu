# Agent checkpoint

## Current status

Frozen m1c-26 is BLOCKED on the missing selected target firewall backend after the approved native transport workaround. A reproducible static AArch64 decision/API prototype plus static native x86_64 NFQUEUE transport were built from `fixtures/recipes/aarch64/network-demo-ipc-m1c26.mk`; all 7 source/recipe/binary SHA-256 entries in `locks/m1c26-ipc-prototype.json` were checked. The opt-in owned-router probe saw a target API initially `queue_ready=false`, native queue-42 bind ACK, `/proc/net/netfilter/nfnetlink_queue` entry, target `queue_ready=true`, and successful no-rule client→server routed HTTP. The next read-only firewall check failed/127 because `/opt/sbin/iptables` does not exist in the target image. No queue rule, target packet decision, packet/rule counter increment, ACCEPT/DROP/restored ACCEPT or pcap was attempted or claimed. Raw `docs/evidence/m1c26-ipc-backend-blocker.json` byte-matches ignored `reports/m1c26ipc-2fa35a1b085f-packet-flow.json`, SHA-256 `c4f2af1804355c3b77304acd95ba4b613c5ba698a5fcd0e6aa4308ad1ac2b25f`; separate `docs/evidence/m1c26-ipc-backend-rollback.json` SHA-256 `af520a6fcc41691edaf56588d89c1fe1f7875d51b9f14d2631879fef6e3cf59e` records scoped rollback. After the owner resources were removed and `xt_NFQUEUE` use count was 0, the explicitly approved disposable network-none helper unloaded only `xt_NFQUEUE` (exit 0); independent readback found module and sysfs absent, `nfnetlink_queue` still loaded, `nft_queue` absent, taint 0 and no KEEMU-owned containers/networks. Portable 202 passed/29 skipped; Ruff, touched formatting and whitespace clean. Before any further rule/packet test, obtain human approval for a pinned target iptables artifact or a different firewall backend within the same exact rule and owner namespace; revalidate module and tool availability, then obtain fresh authorization for any module reloading. Do not treat native bind/target handshake as packet verdict evidence. A14/A17/MVP 1C and m1c-26 remain BLOCKED; no later subtask.

Frozen m1c-26 is approved to resume with a separately reviewed bounded native NFQUEUE transport workaround; it is not yet complete. The project-owned native x86_64 adapter may run only inside the KEEMU-owned router network namespace and may only transport queue packets/verdicts over bounded IPC; the locked AArch64 network-demo must make and count ACCEPT/DROP decisions. The exact project rule remains client `/32` → server `/32` TCP:8080, queue 42, with no queue-bypass and control/health excluded. Read-only preflight confirmed the selected `/opt/sbin/iptables -j NFQUEUE` backend needs `xt_NFQUEUE`; only that module was loaded from SHA-256 `4b1b28c53baa0247a01bce004a029b17a63aca8025cfed7811a7f86bcbcb3b4c`. `nft_queue` remains unloaded, kernel taint remains `0`, and no host-namespace firewall rule, sysctl, public exposure, persistent rule or foreign resource changed; KEEMU-owned containers/networks remain `0/0`. Pre/post evidence is under `/opt/data/backups/keemu-stack-20260922T073131Z/nfqueue-adapter-approval-20260925T064551Z/`; resume backup is `/opt/data/backups/keemu-stack-20260922T073131Z/resume-m1c26-adapter-20260925T064649Z/`. Preserve both prior BLOCKED records and disclose the native transport substitution. Completion requires target decision/counters, kernel/adapter counters, ACCEPT/DROP/restored ACCEPT, pcap, no-listener behavior, ownership-safe cleanup and safe `xt_NFQUEUE` rollback if unused.

Frozen m1c-26 is approved to resume in its existing session after bounded host NFQUEUE preparation and the 2026-09-25 quota reset; it is not yet complete. Fresh live admission at 2026-09-25T06:20:52Z observed 99% remaining with reset `2026-09-25T11:20:52+00:00`. The stale unknown-reset wake at `07:59:32Z` was superseded; the exact waiting supervisor was stopped before same-session restart. `nfnetlink_queue` remains loaded, kernel taint remains `0`, and KEEMU-owned containers/networks remain `0/0`. Original module pre/post evidence is under `/opt/data/backups/keemu-stack-20260922T073131Z/nfqueue-module-approval-20260925T024423Z/`; quota-resume backup is `/opt/data/backups/keemu-stack-20260922T073131Z/resume-m1c26-quota-20260925T062136Z/`. Preserve the historical BLOCKED record, revalidate target queue capability before any exact owner-scoped rule, and require full ACCEPT/DROP/restored ACCEPT, counters, packet evidence, cleanup, exact completion marker and runtime attestation.

Frozen m1c-25 is locally verified on Docker Engine 29.7.2; external supervisor owns runtime attestation and coherent commit/push. `tests/integration/test_m1c25_network_demo.py` ran target AArch64 API-only network-demo on owner-checked client/router/server topology: UI and state GET; POST accept/drop/invalid; HTTP 400 invalid and health 503; refusal of queue rule. Target-selected `drop` mode/file survived service stop/start and same-ID Docker router stop/start with client GET readback. Distinct tracked raw records `docs/evidence/m1c25-web-api.json` SHA-256 `150b32bee85ae0621576fdd29be639b4d1e71b986941d3da5528e6ed8b9ecab4` and `docs/evidence/m1c25-persistence.json` SHA-256 `1728e3977fa3c0ead36c5294d5be3db7683f78dfcfe0d1f540b7e66e21b5eab4` match ignored `reports/m1c25-fba9b9cd5426-*.json`. Scoped A15/A16 API-only slices PASS; whole IDs remain PARTIAL; A14/A17/MVP 1C BLOCKED. Router `br0` was not re-established after environment restart, so no routing or packet proof there. No rule, packet, verdict, kernel counter, host module/firewall mutation or publish. Live 1 passed; final portable 200 passed/26 opt-in skipped; Ruff, touched formatting, diff whitespace clean. Foreign Docker ID sets matched, owner container/network label queries empty. Initial target BusyBox `wget -S` diagnostic exited 139; final evidence used `wget -qO-` and observed actual HTTP 400/503 in stderr. No m1c-26 work.

Frozen m1c-24 fixture implementation is locally verified; external supervisor owns runtime attestation and coherent commit/push. Historical P0 NFQUEUE source/hash remain unchanged; `fixtures/sources/network-demo/network_demo.c` compiles it into a static AArch64 HTTP API/UI/counter/mode fixture. `locks/m1c24-network-demo-aarch64.json` pins exact source/recipe/binary SHA-256 (`360289633e24822fd1f6be298a4deef6fa790188392106bfd44433297c65e617`); `fixtures/network-demo/scenario.json` records exact client→server:8080 queue 42 versus router-LAN control/health:9090 exclusion. Fixture-only owner-checked install/start/stop/remove and fail-closed rule hooks are in `src/keemu/network_demo.py`; no generic CLI network lifecycle claim. Native API-only GET UI/state, POST mode validation and service-restart persistence passed; target API-only client read passed with `queue_ready=false`/zero counters, while real target queue startup failed `Protocol not supported` without installing a rule or sending packets. Tracked raw `docs/evidence/m1c24-network-demo-raw.json` byte-matches ignored `reports/m1c24-ce8bd467f384-network-demo-smoke.json`, SHA-256 `f36913458d18c5ec4a2d2dcf5d1ad859bdabcd09cb2ee88fd707ef17cc5d0006`; lock SHA-256 `a67805db40dff17ea35f9651e1e050166b14610eab60020f2f2b6e6d2e92cf7d`. Focused native 4 passed; opt-in live target 1 passed; portable 199 passed/25 skipped; Ruff, touched formatting and whitespace clean. Live foreign Docker IDs matched pre/post, owner-label queries empty. A15/A16 PARTIAL fixture slices, A14/A17/MVP 1C BLOCKED. No m1c-25 or m1c-26 work; do not conflate API-only with packet processing.

Frozen m1c-23 topology slice is locally complete and verified; external supervisor owns runtime attestation and coherent commit/push. Two opt-in Docker tests passed on Engine 29.7.2: internal LAN/WAN `/24` bridges, client LAN only, server WAN only, router real `br0` and Docker-named `wan0`, default/reverse routes, namespace forwarding, AArch64 web-demo HTTP through router with `ForwDatagrams` 0→10. Two simultaneous runs received disjoint subnets; cleanup of one preserved the other; duplicate-run creation refused; injected post-allocation failure rolled back owned resources. Final foreign Docker container/network ID sets matched the pre-run sets and owner-label queries were empty. Tracked raw `docs/evidence/m1c23-topology.json` SHA-256 `9e8ce09bc0e8f0bc4689af5a03bac36393e58d17309b92b394c3cec30787dfe0`; ADR-0015, progress, limitations and traceability record bounded acceptance. Portable pytest: 195 passed/24 opt-in skipped; live topology: 2 passed; Ruff, touched-file formatting and diff whitespace clean. No management network/host publish, direct host module/firewall mutation, NFQUEUE verdict or m1c-24 work. A13 isolated routing slice PASS but whole ID PARTIAL; A18/A20 PARTIAL; A14/A17/MVP 1C BLOCKED. Historical m1b-22 ledger and supervisor-owned `.agent/STATE.json`/router-events changes preserved.

Frozen m1b-22 three-target acceptance observation is locally complete and verified; external supervisor owns runtime attestation, coherent commit and push. `docs/evidence/m1b22-acceptance.json` is write-once, SHA-256 `cf0dca2c0775fed8b055dd6454ecf4195957eb8dd9048d83751b10509d481841`, with tracked raw JUnit, matrix parent/child, three doctor reports and three-target capability probe. Selected opt-in live suite: 28 passed/0 failed/0 skipped; pre-ledger portable: 187 passed/22 skipped; post-ledger full portable: 189 passed/22 skipped. A01 target ELF/shell/nested execution PASS on the three generic targets through preserved m1a-17/m1b-18 evidence; A02–A06/A10/A11 and MVP 1B remain BLOCKED. Current matrix: complete mapping, real AArch64 child PASS, MIPS/MIPSEL static BLOCKED, parent BLOCKED; actual negative defective-package reports remain FAIL while expected-negative harness tests PASS. A11 process proof is synthetic host-only, not device/target integration. Per-target constrained Docker probes: AArch64/MIPSEL/MIPS target NFNETLINK socket exit 1 `Protocol not supported`, native same-namespace control exit 0 each, no queue/rule/packet/verdict/module mutation, module snapshots identical and owner-verified cleanup. Each doctor returned BLOCKED/4 on worker binfmt visibility, not Docker target failure. Initial probe attempts failed on truncated Docker inspect JSON, read-only-root `docker cp` and unavailable bare `cat`; each leftover resource was inspected and removed, then final probe streamed to bounded tmpfs via target BusyBox. Final owner-label container/network queries empty; no later m1c work started. Focused ledger tests 2 passed; Ruff, touched-file formatter, diff whitespace and tracked-raw hash readback clean. ADR-0014, progress, limitations and traceability updated.

Frozen m1b-21 synthetic event-contract slice is locally verified; external supervisor owns runtime attestation and coherent commit/push. `src/keemu/events.py` offers an explicitly opted-in host-local `python -m keemu.events` runner for SHA-256-pinned synthetic `boot` and `netfilter` fixtures only. It dispatches filename-ordered executable handlers in owner-only fixture directories with explicit cwd/PATH/allowlisted environment, per-handler timeout with process-group kill, nonexecutable skip/fail and stop/continue policy, no inherited environment, redacted JSONL, and exact before/after rule-list state. Real Python subprocess tests restore a test-owned JSON rule list from initial and deliberately changed states and repeat on already restored state without duplicates; unsupported events log BLOCKED/4, failures do not become PASS. No actual firewall, target hook, Docker, physical response, persistent `keemu event`, or A21 bundle is claimed. A12 and MVP 1B remain BLOCKED. ADR-0013, `docs/evidence/m1b21-events.md`, limitations and traceability bind this scope. Focused tests: 19 passed; full portable pytest: 187 passed/22 opt-in skipped; Ruff, touched-file formatter and whitespace checks passed. No later subtask started.

Frozen m1b-20 implementation is locally verified; external supervisor owns runtime attestation and coherent commit/push. `src/keemu/ndm.py` implements strict version-1 exact process argv/response contracts, stateful write/read with no-effect failure, interprocess-serialized and contract-digest-bound local state, redacted JSONL, unknown-call exit 64/event and `required_check` mapping to required BLOCKED/environment-gap rather than package FAIL. Reserved exit codes 64/65 cannot be declared as known read responses. Real host Python subprocess tests use only generated synthetic fixtures; no observed physical Keenetic response, target `/opt/bin/ndmc` executable, `keemu test`/`up` wiring, or event handler is claimed. A11 as a whole remains BLOCKED, MVP 1B incomplete. ADR-0012 and traceability record the bounded slice. Portable pytest: 168 passed/22 skipped; Ruff, touched-file formatter and diff whitespace pass. Read-only Docker owner-label container/network queries empty. No later subtask started.

Frozen m1b-19 is verified locally; supervisor attestation/commit remains external. Strict schema-version-1 matrix with explicit per-case production lock, three-profile completeness, one logical package and preflight architecture/hash validation is implemented in `src/keemu/matrix.py`. CLI `keemu test --matrix FILE [--strict]` visits cases sequentially and writes an aggregate schema-version-2 JSON/Markdown/JSONL report, with per-case immutable child path/hash when executed. Valid architecture mismatch FAIL/1, unsupported target BLOCKED/4, missing target BLOCKED/4, malformed input exit 2, and report priority ERROR → FAIL → BLOCKED → WARN → PASS are covered by portable tests. Real opt-in complete mapping ran AArch64 child PASS and MIPSEL/MIPS static BLOCKED; aggregate BLOCKED, not A10 PASS. Latest parent report `reports/matrix-977fbb17ec7f496cb4d8802e10fd5b2f/report.json` SHA-256 `965c87af8a01a8b6345c9db68ad51b18b628b9ecc38e5f324dc4e07d6276d14e`; child report SHA-256 `c8f728d9e715be64037951dad10b3fc7b7c8f3aebe3f68a47e6d09b390bbb5a1`. Portable pytest 153 passed/22 skipped; opt-in matrix 1 passed; Ruff/format/whitespace clean. Read-only owner-label container/network queries empty. Historical m1b-18 ledgers and prior checkpoints preserved; no MIPS general `init`/`test`, NDM, event or later subtask work. A10 and MVP 1B incomplete. See `docs/evidence/m1b19-matrix.md` and ADR-0011.
Read-only post-verification `keemu doctor --profile generic-mipsel` and `generic-mips` (staged QEMU on PATH) both returned exit 4/BLOCKED on binfmt because this worker cannot see host `/proc/sys/fs/binfmt_misc`; Docker target exec still PASS and external approved host readback remains exact. This doctor limitation is documented, not promoted to PASS.

P0 technical-risk work is complete; m1a-09 input schemas, m1a-10 static IPK inspection, m1a-11 locked AArch64 base init/cache, m1a-12 Docker runtime boundary and m1a-13 disposable one-shot IPK lifecycle are implemented and verified at their exact slices. This is not MVP 1A acceptance: general host-vantage lifecycle and many acceptance facets remain incomplete. P0-01 through p0-06 are verified; p0-07 completed a bounded isolated NFQUEUE diagnosis, not an ACCEPT/DROP proof; p0-08 independently reconciled the evidence package and its digests. Native NFNETLINK socket creation works while static AArch64 socket creation returns `EPROTONOSUPPORT`; the original dynamic target fixture also cannot load against Entware libc. Host NFQUEUE modules are not active and no host-module mutation was approved. A13/A14/A17 and MVP 1C remain BLOCKED; A18 recovery remains PARTIAL. A01 target ELF/shell/nested-exec now has all three generic targets, without promoting other acceptance IDs.

## Verified

- Active branch is `agent/keemu`; the authoritative specification remains unchanged.
- Durable supervisor/router validation passes all 52 tests when run from `/opt/data/hermes-durable-vps-hostinger-router-v1`.
- p0-02 completed with supervisor attestation for `gpt-5.6-sol` / `high`; its disposable worker session was deleted. The then-pending p0-03 boundary was paused for a model-policy refresh before p0-03 and p0-04 later completed.
- Graphify index contains 90 nodes and 86 edges; its query confirmed that the repository initially contained specifications/control files but no product implementation.
- At the initial preflight, Docker CLI 26.1.5 was present but `/var/run/docker.sock` was absent; later checkpoints verify Docker Engine 29.7.2 is reachable through the socket.
- At initial preflight public artifact access worked and no host firewall, binfmt, kernel module, service, public port, OAuth state, credential, or deploy key was changed. A single `qemu-aarch64` host binfmt entry was subsequently installed with explicit user approval and independently verified below.
- `locks/p0-aarch64.json` records 20 real Entware package artifacts plus bootstrap and diagnostic tooling with SHA-256 values derived from downloaded files.
- `uv sync --python 3.12` created a project-local environment using CPython 3.12.13 and generated `uv.lock`.
- `uv run keemu p0 verify-lock --lock locks/p0-aarch64.json --cache .runtime/p0/aarch64-k3.10/packages` verified all 20 packages.
- Direct QEMU/PRoot diagnostic verified target shell, real target `opkg`, nested target ELF execution, target shebang execution, and reported `aarch64`.
- Diagnostic evidence exists at `reports/20260922T080151Z-p0diag-61f167a83d33/` and is correctly excluded from Git.
- `uv run ruff check .` passes.
- Generic AArch64 profile YAML is strict, rejects duplicate keys and target/CPU mismatches, and its committed JSON schema equals `GenericProfile.model_json_schema()`.
- `keemu doctor` builds the common strict `RunReport`; report aggregation implements required-SKIP → BLOCKED and priority ERROR → FAIL → BLOCKED → WARN → PASS. Markdown and JSON use the same report object.
- `keemu doctor --profile generic-aarch64` produced a schema-valid real-mode `BLOCKED` report and exit 4 because `/var/run/docker.sock` is unavailable; no capability was claimed as PASS without evidence.
- Report-status exit codes are centralized: FAIL=1, ERROR=3, BLOCKED=4, PASS/WARN=0. Invalid doctor profile input exits 2.
- Portable suite: 16 passed, 1 diagnostic test skipped by default. `uv run ruff check .` passes.
- The coherent p0-01 implementation and durable state were committed as `573a2df` (`feat: add P0 doctor and report primitives`).
- Strict frozen A21 metadata models cover artifacts, scenarios, profile revision/hash, runtime provenance, capability results, substitutions, coverage, operation records, checks, and partial failures.
- Current `RunReport` schema version 2 validation rejects inconsistent aggregate status or coverage, non-contiguous operation sequences, and partial failures whose referenced operation sequence is missing or disagrees on operation name or failure status.
- Report bundles atomically publish `report.json`, `report.md`, and `operation-log.jsonl` with Linux `renameat2(..., RENAME_NOREPLACE)`; existing directories/files/symlinks survive races, unavailable no-replace support fails closed, and failed-run tests verify all three artifacts survive with cleanup metadata.
- The committed `schemas/run-report.schema.json` matches the expanded schema-version-2 model.
- Real `keemu doctor --profile generic-aarch64` remains truthfully BLOCKED/exit 4 and now includes profile hash/revision, host kernel, Entware target, Python runtime version, and six explicit capability results.
- Portable suite: 25 passed, 1 diagnostic test skipped by default. `uv run ruff check .` passes.
- Final independent staged-diff review found no remaining security concern or blocking logic error after two TDD fix cycles.
- The coherent p0-02 implementation and durable state are committed as `e4d2ad2` (`[verified] feat: expand A21 report metadata primitives`).
- `locks/p0-fixtures-aarch64.json` records a real 12-artifact Debian trixie AArch64 cross-toolchain, project-owned hello/web-demo/NFQUEUE-consumer source and recipe hashes, AArch64 ELF metadata, runtime dependencies, kernel requirements, and reproducibly rebuilt output hashes.
- Rebuilding the three fixture sources with the extracted locked toolchain reproduced their recorded SHA-256 values: hello `334fd4454f3cdbcc4556704dec766e66426e9a75876c1f1f27e6d5fd3156955c`, web-demo `aa45036ae479cc1901492d64d534d72789d48e1e419897ee51ae91d413ffd10b`, and NFQUEUE consumer `0d5a85fabecd3754633fa513712a1cc7b2be2871fd31a9d08c58ae68a15b3886`; `readelf` confirmed ELF64/little-endian/AArch64 for all three.
- `keemu p0 verify-fixture-lock --lock locks/p0-fixtures-aarch64.json --root . --verify-external` verified all sources, recipes, and staged toolchain artifacts. Portable suite: 28 passed, 1 skipped; `uv run ruff check .` passes.
- P0-04 rebuilt the verified 20-package AArch64 rootfs and compiled a static amd64 native PID 1. Docker Engine 29.7.2 built `keemu/p0-aarch64:mixed-v1` as `linux/amd64`; `docker image inspect` confirms `sha256:bfc44a226f962eb35251bb51c33caada4a9e0d44b2bb63567a5fff18b35b1f04`, entrypoint `/__keemu/init`, and explicit owner/target/source/rootfs labels. A saved-layer audit independently found 28 AArch64 ELFs and exactly one amd64 ELF, with target BusyBox `/bin/sh` and target opkg; `verify_image_lock` passed against `locks/p0-mixed-image-aarch64.json`.
- The static container-create template has explicit ownership/run labels, memory/CPU/PID limits, dropped capabilities, no-new-privileges, read-only root and bounded tmpfs; it was not executed. No container, binfmt change, port, or network was created. Portable suite: 30 passed, 2 skipped; explicitly enabled image integration audit: 1 passed; Ruff and `git diff --check` pass.
- P0-05 independently revalidated the locked image with `verify_image_lock` (28 AArch64 ELFs and one amd64 ELF), created and started an isolated, labeled project container from the exact locked image on Docker Engine 29.7.2, and inspected its running state and applied nonprivileged, network-none, memory/PID bounds. `docker exec /bin/sh -c ...` failed with exit 255 and `exec /bin/sh: exec format error`; the native init remained running afterward. `docker stop -t 5` exited cleanly (container exit code 0), then owner/run-id-verified `docker rm` removed it; absence was independently read back. Evidence: `reports/20260923T060100Z-p005-cd7142bf3557/probe.json` (runtime artifact, not committed). Foreign Hermes and Traefik containers remained running; no binfmt/port/host-network setting was changed.
- The Hermes `KEEMU` Kanban board is bound to project `keemu` and contains the full frozen 32-subtask plan in dependency order with stable idempotency keys and explicit model/provider overrides. `preflight` through `p0-05` are marked done; `p0-06` and all later work remain blocked, so Kanban cannot dispatch duplicate work while the durable supervisor is stopped. Verified board totals are 6 done and 26 blocked.
- User-approved bounded host remediation installed only `qemu-aarch64` through pinned `tonistiigi/binfmt:qemu-v10.2.3-68` digest `sha256:400a4873b838d1b89194d982c45e5fb3cda4593fbfd7e08a02e76b03b21166f0`. Independent host-proc readback shows entry `qemu-aarch64` enabled with interpreter `/usr/bin/qemu-aarch64` and flags `POCF`; binfmt status now adds only `linux/arm64` and `qemu-aarch64`. A bounded, nonprivileged, network-none KEEMU-owned smoke container executed target `/bin/sh`, retained the configured memory/PID bounds, and was owner/run-id verified absent after cleanup.
- P0-05 target Docker/binfmt probe `reports/20260923T075238Z-p005-b86b9e5a6c75/probe.json` PASS on the original locked image: target `/bin/sh`, real `opkg --version`, nested AArch64 BusyBox ELF, executable kernel shebang, daemon surviving its `docker exec` parent (adopted by PID 1), short orphan disappearing from procfs after exit, SIGTERM `docker stop` exited/0, owner-checked cleanup absent, Docker daemon survived. The single disposable `/tmp` tmpfs was explicitly mounted `exec` for the shebang test; the original noexec template did not allow executing a test file. No host setting was changed by the probe.
- The separate p0-05 signal differential uncovered a real native-init bug: after killing the keeper child, the original container stayed running for a 10-second `docker wait` timeout (`reports/20260923T075436Z-p005-303e5d4292e3/probe.json`). The keeper inherits PID 1's SIGTERM/SIGINT handlers and loops in `pause()`, so the original image cannot yet prove signal forwarding. The owner-verified signal probe was stopped/removed; no KEEMU container remains. Preserve the original p0-04 source, lock, saved image and prior PASS as historical checkpoints; make a separately locked p0-05 native-init correction, then rerun the full probe and differential.
- The separately locked P0-05 derived image `sha256:66efaedcbd42ac56db30122762ebf73bcfb41a17dd6404bf22f7af067047b6f6` corrected inherited keeper SIGTERM/SIGINT dispositions and verifies forwarded signal by waiting for the keeper's exact signaled exit. `verify()` independently matched live inspect, source hash, original base-layer hash, replacement init file/hash, saved config/archive and labels against `locks/p0-mixed-image-aarch64-p005.json`; original P0-04 image/lock remain unchanged. Full Docker/binfmt probe `reports/20260923T080109Z-p005-e6653acf4ef7/probe.json` PASS: target shell/opkg/nested ELF/shebang, adopted daemon surviving parent, adopted short child reaped, graceful SIGTERM exit 0, unexpected keeper death exit 1, PID 1 SIGINT exit 0, all three owner/run-id checked containers absent, daemon and foreign containers alive. Test-only `/tmp` exec tmpfs was bounded to the disposable shebang container; no host setting changed in this probe.
- Independent report assertions and `verify()` PASS; portable pytest 30 passed, 2 skipped, Ruff clean and `git diff --check` clean. Neither A01 nor A18 as a whole is PASS; P0 web/persistence/NFQUEUE still unrun.
- The final local p0-05 probe JSON SHA-256 is `86a81d2c33de6c68663871f4e2ff49e1c1c0920055408e123169744a9d52209b`; the retained failing original-image keeper differential JSON SHA-256 is `169c6b5ab6ecd6ed6db45d7b7e15d875818158b81ce8cc8b57c9632c876dbc61`. Both are under ignored `reports/`, not committed.
- Supervisor runtime now supports `pause_after_subtask_ids`; KEEMU config pauses after `p0-05`. Compile, JSON validation, and all 71 supervisor/router tests pass. This closes the prior auto-advance gap: an attested p0-05 completion will activate but not launch p0-06.
- P0-06 added a separately locked AArch64 `web-demo-p006` fixture and `keemu/p0-aarch64:web-p006` derived image. The target binary is static because the real Entware libc rejected the initial dynamically linked build (`GLIBC_2.34` unavailable); the locked static binary is `0856c7af577b6fb1675ca6266686853863aee23e1cbe15a207c64ed0089cbf36`. `locks/p0-web-demo-aarch64-p006.json` binds the source, recipe, binary, derived image, saved archive and immutable p0-05 base.
- P0-06 PASS evidence is `reports/20260923T125140Z-p006-aa4677e64b41/probe.json` (SHA-256 `af6a9d03c1f084ba3257cbfab4f81de8aa4af761bbc62ae66de362ba897fd669`). It independently verified the application image, its exact `127.0.0.1:18080→8080/tcp` and `127.0.0.1:18081→8081/udp` Docker publishes, and matching `docker port` output. The bounded bridge-network application container had no bind mounts, no privilege, dropped capabilities and fixed resource limits.
- The approved host-vantage proof uses a separate, static amd64 scratch image locked in `locks/p0-host-observer-p006.json`: source SHA-256 `ddcbf1c7320d504ad55f9943b76da680f06d4f40f495009d2780ab4cf64f878d`, binary SHA-256 `7a97b020802ae68fb83c34f172a6823a5ab4ebec33f36d80a814f482900b9b9d`, image `sha256:bbd0043b74fdcd0ba599abd9865823c8c2f222ba3e129a92b9cb6aa5acf2c53a`. Each transient observer had only the explicitly authorized `--network=host` vantage, owner/run-id labels, read-only root, no port publish/binds/privilege, dropped capabilities and bounded resources. It proved HTTP health and UDP echo through host loopback at initial state, after state write, after service restart, and after Docker stop/start. Every observer and the application container was ownership-checked, removed, independently absent; a post-run Docker label query found no P0-06 or KEEMU-owned container.
- State `p006-aa4677e64b41` was written to `/opt/etc/web-demo/state.txt`; its SHA-256 `bafa47ea6c257db706ecedd28d7f790d266249ae09275847614165b8e7936926` matched before service restart and after the same labeled container exited and restarted. The final host-vantage HTTP and UDP result remained `HTTP_OK UDP_OK state=p006-aa4677e64b41`.
- P0-07 ran `uv run python tests/integration/p0_nfqueue_preflight.py` with one disposable, labeled, network-none, read-only, cap-drop-all Docker container, derived from the locked P0-05 image. `reports/20260923T130017Z-p007-1ed1391a9dd4/preflight.json` is BLOCKED (SHA-256 `b858e1629dd05c9af4ce8f82c9e33a6ee7cecc83a533716fc68844b18599049b`). Native static `NETLINK_NETFILTER` socket opened; static AArch64 socket returned `Protocol not supported`; the locked dynamic target failed `GLIBC_2.34` and a static target consumer executed argument validation only. The native consumer likewise executed argument validation only. No queue binding, ACCEPT/DROP verdict, packet, firewall rule, host module load, privileged container, host network, or published port occurred. Host module snapshots before/after match (NFQUEUE modules absent). Owner-checked stop/removal and independent label queries found no KEEMU container or network. The diagnostic image ID is `sha256:a9a75dfed53f6cd1c4594c3e110582fb6869500e4a3fe1c988fd18684faa9894`; it is not an accepted network runtime.
- P0-07 reproducible source and probe are `fixtures/sources/nfqueue/nfqueue_socket_p007.c` and `tests/integration/p0_nfqueue_preflight.py`; `docs/decisions/0004-p0-nfqueue-gate.md` records the explicit gate. Full portable pytest: 30 passed, 3 skipped; Ruff, formatter check, and diff whitespace check passed. This is a bounded blocker, not A13/A14/A17 acceptance.

## Acceptance truth

- P0 is complete only as the four-condition technical-risk gate defined in `TASK.md`: target execution, localhost publishing/persistence, reproducible NFQUEUE blocker, and committed locks/ADRs. The digest-bound package is `docs/evidence/p0-evidence-manifest.md`.
- A07/A08/A09: PASS for the exact AArch64 P0-06 fixture slice only. Their general lifecycle features remain unimplemented.
- A01/A18/A19/A20/A21 remain PARTIAL. A13/A14/A17 remain BLOCKED; native socket success and static argument parsing are not NFQUEUE verdict evidence. Every acceptance ID's exact status and gap is maintained in `docs/traceability.md`.
- m1a-09 implemented strict schema-version-1 scenario, production scenario-lock and persistent-environment input models; generated schemas `schemas/scenario.schema.json`, `schemas/scenario-lock.schema.json`, and `schemas/persistent-environment.schema.json` pass model parity. The shared YAML loader rejects duplicate keys and aliases, JSON parsing rejects duplicate keys and non-finite numbers, and file sizes are bounded. Scenario-relative file inputs reject project escapes, missing files and symlink components; target mutation paths are canonical `/opt` descendants. Explicit argv shell mode, pinned-installer required commands/source reference, localhost-only TCP/UDP publication, observation vantage, CA-backed HTTPS, source/scenario SHA-256 binding, and environment identity constraints have negative/positive unit cases. Historical P0 locks and P0 runtime evidence are unchanged.
- Portable suite after m1a-09: 59 passed, 3 skipped; `uv run ruff check .`, formatting of touched Python files and `git diff --check` pass. No Docker runtime/registry mutation or new host resource was requested or claimed. A18/A21 remain PARTIAL.
- Validation-to-use race safety, profile/feed/OCI/QEMU live verification, label reconciliation, atomic registry writes, full lifecycle and cleanup are explicitly later work, not proofs from these schemas.
- m1a-10 implements read-only, in-memory `keemu inspect PACKAGE` with optional strict profile and dependency-inclusive rootfs. Signature-based ar/gzip/xz/bzip2/plain-tar detection validates `debian-binary`, unique control/data archives, tar end markers, archive/member/expanded-size and entry-count limits, `/opt` data confinement, duplicate/path/symlink/hardlink/special-file defenses, control metadata/Depends and maintainer scripts, ELF machine/class/endian/OSABI/flags/PT_INTERP/DT_NEEDED/RPATH/RUNPATH, target-rootfs library lookup, shebang and permission checks. No untrusted extraction, host `ldd`, package execution, Docker mutation or host configuration change was performed.
- Adversarial synthetic tests cover format variants, unsafe paths/links/special members, duplicate metadata and entries, corrupt/trailing compression, size limits, malformed ELF, architecture/ABI mismatch, missing loader/library with and without dependency-complete rootfs, postinst deferral, unknown RUNPATH, bad shebang, modes and hardcoded path warnings. Portable inspection tests: 26 passed; full portable pytest: 85 passed, 3 skipped; Ruff and whitespace checks pass. Exact locked P0 cache verification matched 20 packages; static inspection of all 20 against the AArch64 rootfs yielded 16 PASS, 2 WARN and 2 BLOCKED (non-`/opt` interpreter paths need a complete target environment), no false package FAIL. Real CLI `inspect` of locked opkg IPK returned static PASS/exit 0. These are static inspector statuses, not A01 or A02 acceptance results.
- `docs/architecture.md`, `docs/progress.md`, `docs/traceability.md` and `README.md` now distinguish static checks from installed/runtime acceptance. A01 remains PARTIAL, A02 remains NOT RUN; MVP 1A is not complete. Install-time reopen/rehash and target runtime proof are later frozen subtasks.
- m1a-11 `keemu init --profile generic-aarch64 --locked` verified the SHA-256-pinned 20 IPKs, feed index, bootstrap opkg, QEMU DEB/binary, corrected native init source and compiled binary, then installed the closure with target opkg. It checked exact 20-package `list-installed` inventory and wrote default `/opt/etc/opkg.conf`; only volatile `Installed-Time` fields were normalized. Saved scratch `linux/amd64` image auditing matched every file/link/mode with the sole explicit BusyBox setuid attenuation. `locks/m1a-init-aarch64.json` pins cache schema 4, rootfs tree `019311323a4607edcf952dbffcd6c8b96e99b12c26fde2ca463314c1083f54d0`, saved archive SHA-256 `555f3cb5b9277f2c5e3e81b5409ce7bcc35a2a6bdc131ec6641ce3593f3230e9`, inventory and Docker-local image ID `sha256:8f91e88ba865d6eea1f37b3d592fdd8c788273c11202a9e4194ff6c5ef4e6224`. Two independently staged builds matched all recorded hashes. Bounded, owner/run-id-checked Docker/binfmt smoke passed target shell, opkg inventory, nested ELF and bridge DNS; default CA/hostname HTTPS passed only from the Hermes process namespace, not target. Atomic no-replace cache publication and offline prepared-cache revalidation passed; five copied-cache corruption variants failed closed. The ignored evidence `reports/20260923T202746Z-m1a11-base-init/init-evidence.json` has independently recomputed SHA-256 `f30368febcceb1837f036aeb8a1fe1ed7f9015327c455fcaf9f2c14d1de056ff`.
- m1a-11 portable pytest: 89 passed, 10 skipped (seven opt-in m1a-11 integration cases plus three unrelated prior opt-in cases); explicitly enabled real Docker/binfmt m1a-11 integration: 7 passed. Ruff, formatter check, and diff whitespace check passed. Docker inspect re-read the pinned image ID and `linux/amd64` target/native labels; owner-label query found no residual KEEMU container. No host binfmt/firewall/modules/ports were changed by this slice.
- MVP 1A, MVP 1B, MVP 1C, and MVP 1 are not complete.
- m1a-12 adds `src/keemu/docker_runtime.py`: argv-only bounded Docker command capture, exact local image-ID/target/entrypoint validation, project/run/kind/base/target-labeled containers and bridge networks, fresh writable container layers, explicit 2 CPU/1 GiB/256 PID limits, dropped caps, no-new-privileges, bounded tmpfs and 20 MiB JSON daemon log file; full-ID re-inspection gates every mutation and read-only reconciliation reports owned/missing/unexpected resources. `docker inspect --size` checks an observed 512 MiB writable-layer threshold before start and before/after exec; this is not continuous enforcement or a hard disk quota. Log retrieval has a per-stream 20 MiB cap and marker on retrieval truncation, but Docker daemon rotation can discard old logs without an observable marker. No registry or application lifecycle was implemented.
- m1a-12 opt-in live Docker test on Engine 29.7.2 and locked image `sha256:8f91e88ba865d6eea1f37b3d592fdd8c788273c11202a9e4194ff6c5ef4e6224` passed owner-labeled network/container create and inspect, target shell writable state, same-container stop/start persistence, fresh-container isolation, foreign-run cleanup refusal, owner-verified cleanup and independent absence. Ignored report `reports/m1a12-8055e7faa94b-docker-boundary.json` has SHA-256 `d56d3e156876a139c970fda7b72259dc150a1317d8649fa2cabbba7fdf240bf4`. Portable pytest 97 passed, 11 skipped; focused opt-in/live plus unit tests 9 passed; Ruff, formatter and whitespace checks clean. Docker owner-label queries returned no KEEMU container or network. A18/A20 remain PARTIAL; interruption, registry and host disk safety are not proven.
- m1a-13 `keemu test` validates and no-follow rehashes exact scenario/profile/production lock/IPK/feed/base bindings; statically inspects a private copy; offline-verifies the locked AArch64 image; refuses existing run-owned resource collision; hashes target-staged IPK copy-back; installs with target opkg; checks inventory/files, explicit commands and target-loopback HTTP service readiness/stop; removes package and compares metadata-only Docker diff against allowed paths. Only the disposable target gets a checked BusyBox gzip link (report substitution). Every normal failure path attempts owner-checked cleanup and write-once JSON/Markdown/JSONL with validated input bytes, bounded captured command output, exact primary operation and independent cleanup status. No registry, host publishing, target HTTPS/UDP or cross-target result is claimed; see ADR-0007 and `docs/traceability.md`.
- m1a-13 portable suite: 106 passed, 13 skipped; real Docker/binfmt opt-in lifecycle suite: 6 passed. The live cases cover hello/postinst-once, failed postinst, web-demo target-loopback readiness and connection refusal after stop, failed startup, command timeout and an injected cleanup-reporting error after actual removal. Static defect and malformed-package CLI cases produce retained failure reports before Docker; run-ID collision never mutates the pre-existing resource. Ruff, touched-file formatter and whitespace checks passed. Latest ignored reports include `reports/m1a13-service-4a0232069fa9/report.json` (PASS), `reports/m1a13-startup-1db25d1fe79f/report.json` (FAIL), `reports/m1a13-timeout-b43182e02d89/report.json` (ERROR), and `reports/m1a13-cleanup-53a8a4fdc6af/report.json` (primary install FAIL, cleanup ERROR, overall ERROR). Final Docker owner-label queries found no KEEMU container/network. A02–A06, A18 and A21 are PARTIAL as full acceptance IDs.
- m1a-14 implemented an owner-only per-name flock registry with no-replace creation, fsynced atomic JSON transitions, SHA-256-bound scenario snapshots and permanent tombstones. Guards reject symlinks, partial/replaced records (including identical-byte inode swaps), Docker label/ID mismatch and concurrent same-name mutation; different names remain independent. CLI `up --scenario/--lock --name`, `up --name`, `down`, `restart`, `destroy`, `status`, `ports`, `logs` and `exec` use actual Docker state and owner-checked full IDs. No host publish, added capability or automatic stale-state adoption is allowed; see ADR-0008.
- m1a-14 live Docker/binfmt opt-in tests: 3 passed (hello CLI creation/exec/restart/down-up/tombstone with postinst counter unchanged; web-demo target-loopback readiness across same-ID restart/down-up; failed postinst retained `failed` registry state with owner-verified container absence and CLI status exit 3). Static wrong-architecture/shebang cases fail before registry/Docker. Cross-process flock and replacement refusal passed in portable tests. Final portable suite after independent review fixes: 124 passed, 16 opt-in skipped; Ruff, touched-file formatter and whitespace checks passed. Review-driven regressions now cover secure scenario/profile byte snapshots, every transitional status including resource-less `creating`, symlinked registry ancestors, pre-allocation YAML bounds and locked cache-key containment. Independent review passed with no security concern or blocking logic error. Docker owner-label container/network queries showed no KEEMU resources. Current Hermes namespace did not expose a readable qemu-aarch64 binfmt entry; real Docker target execution passed. A03–A06/A18 remain PARTIAL as whole IDs; persistent CLI failure report bundle and interruption/retry recovery are not claimed.
- m1a-15 fixture-only AArch64 TLS frontend source and offline recipe are pinned in `locks/m1a15-https-frontend-aarch64.json` with source/recipe/binary and three arm64 SDK archive SHA-256 values. A clean offline rebuild reproduced static ELF64 AArch64 binary `857719c919b01eb1091c83ef31a620ec06e0849d51ce7e56ced449cd95b3b4f0`. Real opt-in Docker host-vantage HTTP/HTTPS/UDP test generated an ephemeral local CA, validated hostname/chain (negative untrusted CA and wrong hostname), wrote state through HTTPS, rechecked through 127.0.0.1-only publishes after service restart and same-container Docker stop/start, then owner-verified target/observer absence and private-key deletion. Evidence `.runtime/m1a15/live-m1a15-0303315534cc/evidence.json` SHA-256 `6758497e7db513891b6f2f9ae089518903293edda6793cee50172e625d8843a3`. Dynamic host ports changed after container restart and were reread before observation. Separately, two real locked hello one-shot runs against offline-verified base and identical scenario/IPK hashes passed with postinst-once, executable, removal, reports and owner cleanup; exact reports/hashes are in `docs/evidence/m1a15-evidence-manifest.md`. Combined live lifecycle suite 13 passed; portable 124 passed, 18 opt-in skipped; Ruff, formatter and whitespace checks passed, owner-label container/network queries empty. ADR-0009 and traceability explicitly limit this fixture probe: generic `keemu test`/`up` still reject host publish/HTTPS/UDP and full A02–A09/A19 remain PARTIAL. No later subtask started.
- m1a-16 verified explicit bounded persistent recovery: `keemu recover NAME --yes` refuses healthy and ambiguous/foreign state; SIGKILL of the real creator after Docker allocation before ID publication and again after ID publication/start produced inconsistent records that were owner-checked, removed, tombstoned and idempotently reconciled. An injected first removal failure was retried without deleting another run's resource; real Docker name collision, stale registry pointing to a foreign run's ID, and foreign container/network mutation were refused. The other run stayed running and unrelated Hermes/Traefik containers and Docker networks matched exact pre/post identity/state snapshots. Target `CapEff=0`, Docker socket absent, TCP/UDP endpoint tables empty; Docker inspect showed no privilege/host network/binds/added caps/ports with applied 2 CPU/1 GiB/256 PID and bounded logs. Latest ignored `.runtime/m1a16/m1a16-d5b0e7bc6100.json` SHA-256 `8995baa69abc7510c37d4a174320ec9cc127153197cab9c311e6ac22ec53b6a0`; earlier reviewed evidence remains `.runtime/m1a16/m1a16-66c067937562.json` SHA-256 `895ef930d18519d6fa5d85092c195bb78db0735b1faa985def3f15f79de4b1e9`. Opt-in recovery + lifecycle + Docker boundary suite: 13 passed. Final portable suite: 130 passed, 19 opt-in skipped. Ruff, touched-file formatting, diff whitespace and owner-label container/network absence checks passed. One-shot SIGKILL recovery/report, automatic startup recovery, full socket census and hard/continuous disk quota remain absent; A18/A20 are PARTIAL as whole IDs. ADR-0010, limitations and traceability record this exact boundary. No m1a-17 work started.

## m1a-17 verified observation

Live opt-in suite: 22 passed, 0 failed, 0 skipped. Pre-ledger portable: 130 passed, 19 skipped. Post-ledger portable: 132 passed, 19 skipped. Ledger SHA-256 `b8c6fea25231dc5fbe4f30b3607fc9d289d3f5deef706e44369d19ac27b829be`; latest ignored TLS evidence `.runtime/m1a15/live-m1a15-fe12840358aa/evidence.json` SHA-256 `4e55508cc1f85209f8fb4d2cc24049ad64950bd6a181da154a6ca77df8f92237`; recovery evidence `.runtime/m1a16/m1a16-3c761d203e67.json` SHA-256 `f53cd9ff5c7ef0b4dbfaed66ccb71275dcaef01d7883da5c1fcafa3cdeeeacf2`. Recovery probe foreign before/after matched; owner-label queries found no KEEMU container/network. Ruff, touched-file formatter and whitespace checks clean. No m1b-18 work started.

## Next operation

Resume m1c-26 in session `20260925_021217_b58cec` with the existing model lock after fresh quota admission. Implement and review the bounded native adapter/target IPC contract first, proving the AArch64 process selects and counts each verdict rather than merely exposing an API. Then run only the exact owner-scoped queue-42 flow and require bounded ACCEPT/DROP/restored ACCEPT, target/adapter/kernel counters, pcap and no-listener evidence. Remove every KEEMU-owned rule/resource, verify foreign resources unchanged, and unload only `xt_NFQUEUE` if its use count is zero and rollback is safe. Do not load `nft_queue` or other modules, alter QEMU/seccomp, create host-namespace/persistent rules, expose public ports, claim direct target Netlink compatibility, or run later subtasks. Supervisor owns attestation/commit/push after exact verified completion.

Kanban reconciliation remains external to this evidence package.

## Blockers

Revalidate all three approved binfmt handlers, `nfnetlink_queue` and `xt_NFQUEUE` after reboot or runner change. Two direct-target m1c-26 BLOCKED results remain immutable historical evidence. The workaround may prove AArch64 verdict participation only through the declared native transport substitution; it must not be reported as direct target Netlink compatibility. Approval excludes `nft_queue`, arbitrary modules, host-namespace or persistent firewall rules, sysctls, QEMU/seccomp changes and public exposure.
