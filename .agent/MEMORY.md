# Agent memory

- KEEMU is implemented in `/opt/data/workspace/keemu`; `/opt/data/hermes-projects/keemu` is only a compatibility symlink.
- `KEEMU_MVP1_updated.md` revision 2.2 is the authoritative specification.
- Development proceeds by vertical gates P0 → 1A → 1B → 1C and uses acceptance IDs A01–A21.
- A successful generic target test does not prove compatibility with a physical Keenetic model.
- The current Hermes container has usable access to the host Docker daemon through `/var/run/docker.sock`; on 2026-09-23 it verified Docker Engine 29.7.2. This is privileged host access, and binfmt/privileged container actions still require separate explicit approval.
- The private remote is `git@github.com:YOZH3G/keemu.git`. Repository-local SSH uses only `/opt/data/.ssh/id_ed25519_keemu`; never store the private key in Git. Verified subtask checkpoints auto-push `agent/keemu`, while auto-pull, force-push and automatic merge remain disabled.
- The first P0 lock is `locks/p0-aarch64.json`: 20 Entware `aarch64-3.10` packages plus hashed bootstrap and diagnostic tooling.
- Direct QEMU 10.0.13 with PRoot 5.1.0 can execute the locked AArch64 shell, real opkg, nested target ELF and target-shebang smoke test; this is diagnostic evidence only and does not satisfy A01 or P0.
- Generated runtime artifacts live under `.runtime/`; generated reports live under `reports/`; both are excluded from Git.
- `profiles/generic/generic-aarch64.yaml` is the strict current generic profile; `schemas/generic-profile.schema.json` is generated from `GenericProfile` and parity-tested.
- `keemu doctor` emits current schema-version-2 `RunReport` JSON; its earlier BLOCKED/exit-4 evidence reflected a then-absent Docker socket and must not be mistaken for the present Docker capability. Docker, binfmt, and QEMU checks are real-mode host probes.
- Run reports use frozen nested metadata and derived status/coverage validation. Partial failures reference an exact operation-log sequence whose operation name and failure status must match. `write_report_bundle` atomically creates a new immutable run directory containing `report.json`, `report.md`, and `operation-log.jsonl`; existing directories are never overwritten.
- Future Luna/Sol assignments use `gpt-6-luna`/`gpt-6-sol`; historical attested events retain actual pre-refresh model IDs. The p0-03 `gpt-5.6-terra`/`high` outcome was attested; p0-04 and the active p0-05 have `gpt-6-sol`/`high` locks, with runtime attestation required by the supervisor at boundaries.
- `locks/p0-fixtures-aarch64.json` locks the project-owned AArch64 hello, web-demo and raw NFQUEUE-consumer fixture sources/recipes plus a 12-DEB Debian cross-toolchain; `keemu p0 verify-fixture-lock --verify-external` validates their hashes without network access.
- `locks/p0-mixed-image-aarch64.json` records a real Docker-built `linux/amd64` image ID, native static amd64 init, AArch64 Entware rootfs, saved layer/config hashes and whole-image architecture audit (28 target, one native ELF). `verify_image_lock` checks the live image and saved archive. Container limits are only an unexecuted create template; no p0-04 container/runtime acceptance is claimed.
- The original P0-04 image/lock/source remain immutable. `locks/p0-mixed-image-aarch64-p005.json` pins a derived image with only a corrected static native init overlay; `verify()` in `src/keemu/p0_runtime_image.py` checks the original base layer, replacement file and live/saved image. The explicit `tests/integration/p0_runtime_probe.py` tests Docker/binfmt target shell/opkg/child/shebang and native init daemon survival, forwarding/reaping and clean owner-checked shutdown; its generated evidence is under ignored `reports/`. AArch64 host binfmt registration was separately approved and installed with `qemu-aarch64` flags `POCF`; revalidate after host changes.
- P0-07 bounded capability diagnosis is reproducible with `tests/integration/p0_nfqueue_preflight.py`, which uses a project-labeled, cap-drop-all, network-none container and writes ignored evidence. On the current host the native `NETLINK_NETFILTER` socket opens, the static AArch64 equivalent returns `EPROTONOSUPPORT`, and the original locked dynamic target consumer fails the Entware `GLIBC_2.34` requirement. The host NFQUEUE modules were not active and no module/rule/queue mutation was approved or performed; A13/A14/A17 and MVP 1C remain BLOCKED. See `docs/decisions/0004-p0-nfqueue-gate.md` before proposing further tests.
- P0 technical-risk closure is documented in `docs/evidence/p0-evidence-manifest.md`: exact report and lock SHA-256 values were recomputed on 2026-09-23. P0 completion is not acceptance completion: A07/A08/A09 pass only for the locked AArch64 P0-06 slice; A01/A18/A19/A20/A21 remain PARTIAL; A13/A14/A17 and MVP 1C remain BLOCKED. The explicit Docker-host observer is evidence-only; see `docs/decisions/0005-p0-localhost-observer.md`.
- MVP 1A input contracts live in `src/keemu/scenarios.py`, `input_locks.py`, `input_paths.py`, with generated JSON schemas in `schemas/`. Production schema v1 does not reinterpret historical P0 locks. Loaders reject duplicate keys/aliases, unknown versions/fields, path escapes/symlinks, source/scenario hash mismatches, and public publishes; they do not run containers or authorize registry mutation. Runtime callers must recheck at use and prove Docker labels/digests separately.
- `src/keemu/ipk_inspect.py` and `keemu inspect` implement bounded, no-extraction static IPK triage: magic-detected ar/tar wrappers, control/data metadata, architecture/ELF/interpreter/needed-library/search-path/shebang/mode/link checks. A complete dependency-inclusive target rootfs is needed for definitive missing-library FAIL; postinst and out-of-scope loader paths may remain BLOCKED. Static PASS does not prove opkg installation, A01/A02 acceptance or input integrity at later use. Reopen/rehash at install time; see D-016.
- `src/keemu/init_cache.py` and `keemu init --profile generic-aarch64 --locked` prepare the AArch64 base only from exact P0 package/index/bootstrap/QEMU inputs and the corrected P0-05 native init. `locks/m1a-init-aarch64.json` pins cache schema 4, installed-package inventory, default opkg config, rootfs/image/archive hashes and Docker-local image ID `sha256:8f91e88ba865d6eea1f37b3d592fdd8c788273c11202a9e4194ff6c5ef4e6224`; two independent builds matched. `.runtime/init-cache/<key>/` is atomically no-replace published after bounded Docker/binfmt smoke, and `--offline` verifies prepared inputs/cache/image with no fetch or build. Host/binfmt image must remain available for repeat; target DNS/exec/opkg inventory passed, but HTTPS was only Hermes-process-vantage, not target. `reports/20260923T202746Z-m1a11-base-init/init-evidence.json` SHA-256 `f30368febcceb1837f036aeb8a1fe1ed7f9015327c455fcaf9f2c14d1de056ff` is ignored runtime evidence. A01/A19 as a whole remain PARTIAL; application lifecycle is later work. See D-017.
- `src/keemu/docker_runtime.py` is the m1a-12 argv-only Docker boundary: exact Docker-local image ID and project/target/native-init check; run/kind/base/target-labeled owned containers and bridge networks; fixed 2 CPU/1 GiB/256 PID, no privilege/host namespaces/binds/volume/socket, bounded tmpfs and 20 MiB single-file JSON logs; full-ID ownership verification before mutation, read-only reconciliation, and observed 512 MiB writable-layer threshold. Live AArch64 image test proved fresh-layer separation, same-ID persistence, foreign-run refusal and owner-only cleanup; `reports/m1a12-8055e7faa94b-docker-boundary.json` SHA-256 `d56d3e156876a139c970fda7b72259dc150a1317d8649fa2cabbba7fdf240bf4`. Daemon log rotation can lose history without a marker and disk threshold is not continuous/hard. Registry, lifecycle, interruption recovery and full A18/A20 acceptance are later work; see D-018.
- `src/keemu/lifecycle.py` and `keemu test` implement the m1a-13 disposable one-shot AArch64 IPK pipeline: no-follow use-time input/target-stage hash checks, static inspection, offline base validation, real target opkg install/remove, package inventory and checks, target-loopback HTTP readiness/refusal on stop, metadata-only residual diff and owner-verified finally cleanup. Missing target gzip link is added only to the fresh container and reported as a substitution. Reports retain primary failure and independent cleanup outcome, bounded captured command output and input bytes. Real opt-in Docker/binfmt suite passed; portable suite passed. This does not implement registry/recovery, publish/HTTPS/UDP, full byte/sockets census, cross-target acceptance or full A02–A06/A18/A21 proof; see D-019 and ADR-0007.
- `src/keemu/registry.py` and `persistent.py` implement m1a-14 owner-only per-name flock, atomic no-replace creation, fsynced schema-v1 state transitions, SHA-256-bound scenario snapshots, name tombstones and exact Docker ID/label/base/state reconciliation for bounded AArch64 persistent IPK operations. CLI supports `up` creation/resume, `down`, `restart`, `destroy`, `status`, `ports`, `logs`, `exec`. Real Docker tests passed hello persistence/postinst-once, web-demo readiness across restart/down-up and failed-postinst cleanup; portable tests cover cross-process locking, symlink/inode replacement and foreign Docker refusal. Persistent CLI does not produce report bundles, host publishes, HTTPS/UDP, cross-target execution, automatic interruption recovery or full A03–A06/A18 acceptance; see D-020/ADR-0008.
- `fixtures/sources/web-demo/https_frontend_m1a15.c`, offline `fixtures/recipes/aarch64/https-frontend-m1a15.mk` and `locks/m1a15-https-frontend-aarch64.json` bind a static AArch64 TLS frontend and three pinned local arm64 SDK archives. `tests/integration/test_m1a15_https.py` opt-in verifies generated local CA chain/hostname, HTTP/HTTPS/UDP on explicit Docker-host-loopback publishes, state through HTTPS across service and same-container restart, plus two identical offline one-shot hello installs/removals. Host observer is run-owned/read-only/cap-drop-all and removed; private keys stay ignored and are deleted. Evidence/hashes: `docs/evidence/m1a15-evidence-manifest.md`; ADR-0009. Generic `test`/`up` still reject host publish/HTTPS/UDP; A02–A09/A19 remain PARTIAL overall.
- `keemu recover NAME --yes` is an explicit m1a-16 path for failed/interrupted persistent AArch64 environments, not automatic adoption or opkg replay. It requires per-name flock plus Docker full-ID/run/base/target label checks; an unrecorded container must match the deterministic run name. A recorded but relabelled/foreign ID and any extra owned container/network fail closed; verified cleanup retains a tombstone and retries are idempotent. `tests/integration/test_m1a16_recovery.py` SIGKILLed the real creator before ID publication and after start, verified retry/collision/stale-registry/foreign-resource isolation, target zero effective caps, absent Docker socket and empty TCP/UDP tables, and inspected applied resource limits. Evidence `.runtime/m1a16/m1a16-d5b0e7bc6100.json` SHA-256 `8995baa69abc7510c37d4a174320ec9cc127153197cab9c311e6ac22ec53b6a0`; ADR-0010. One-shot SIGKILL journal/report, automatic recovery, hard disk quota and full socket census remain absent; A18/A20 PARTIAL.
- `docs/evidence/m1a17-acceptance.json` freezes an AArch64 acceptance observation (SHA-256 `b8c6fea25231dc5fbe4f30b3607fc9d289d3f5deef706e44369d19ac27b829be`). The project-clean shared Docker/binfmt host ran 22/22 live tests and the pre-ledger portable suite 130 passed/19 skipped; post-ledger suite 132 passed/19 skipped. Whole A01–A09/A18–A21 and MVP 1A remain BLOCKED; scoped AArch64 checks PASS, negative defective-package reports FAIL, MIPS/MIPSEL/fresh-Ubuntu checks SKIP. Not a clean Ubuntu VM or general published lifecycle. `tests/integration/freeze_m1a17.py` refuses overwrite; ignored raw evidence is hash-bound in the ledger.
- m1b-18 MIPS/MIPSEL preparation has per-target SHA-256-locked 20-package Entware roots and 16-package target-GCC SDK closures, three target-built fixtures each, 31 audited MIPS ELF32/o32/MIPS32r2/soft-float files per mixed image, and `profiles/generic/generic-mips*.yaml`. `scripts/verify_m1b18.py` checks cached index/closure/SDK/fixtures, rootfs/target opkg inventory, saved image file-by-file and local Docker image. Direct QEMU/PRoot shell, nested ELF/shebang, opkg and fixture argument checks passed; Docker target exec failed `exec format error` for both on this host. `docs/evidence/m1b18-targets.json` SHA-256 `816727a7b0ea632946cbca3dcad6a2f75b286df7916f6705c34a9b16c8ea8f0c` records BLOCKED A01 slice; A10 matrix remains NOT RUN. No MIPS binfmt registration was approved or changed. `keemu init`/`test` remain AArch64-only. Resume same m1b-18 only after explicit host-binfmt approval or a preconfigured runner; do not advance m1b-19.
