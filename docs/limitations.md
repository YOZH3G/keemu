# Limitations

## Current verified boundary

The P0 technical-risk gate has real AArch64 evidence for Docker/binfmt target execution, localhost-only HTTP/UDP publishing, and state persistence across service restart and Docker stop/start. Its NFQUEUE requirement is closed as a reproducible BLOCKED capability diagnosis, not an ACCEPT/DROP result. Exact inputs and report digests are in `docs/evidence/p0-evidence-manifest.md`.

Final release status is BLOCKED. `docs/evidence/final28-acceptance.json` gives
whole-ID semantics: A01 PASS only for the specified three-generic-target
execution check; A02–A21 remain BLOCKED. `docs/evidence/final29-release.json`
records a retained full sweep of 233 PASS, 2 prerequisite FAIL and 3 intentional
SKIP cases, plus a fresh portable 208 PASS/30 opt-in SKIP run. The failed cases
need absent Docker-local historical P0/TLS image IDs; their saved export config
digests differ, so no import or replacement was attempted. Current owner
containers/networks are 0/0; `nfnetlink_queue` remains loaded while
`xt_NFQUEUE`, `iptable_filter` and `nft_queue` are absent. Fresh Ubuntu and a
new packet-verdict job are NOT RUN. Repository-wide formatter check still fails
on six unchanged committed files.

## Known limitations

- Docker Engine 29.7.2 is reachable through a privileged host socket. The separately approved `qemu-aarch64` binfmt registration must be revalidated after reboot or runner change. No privileged target container or public port was used.
- P0-05 proves AArch64 target shell/opkg/child/shebang execution and bounded native-init signal/reaping behavior. It does not prove cross-target support or interruption/stale-resource recovery. The direct-shebang check used an explicitly executable disposable `/tmp` tmpfs because the baseline noexec tmpfs cannot run test files.
- P0-06 proves only the locked web-demo slice: explicit `127.0.0.1` TCP/UDP publishes, Docker-host-vantage HTTP/UDP observation through an explicitly approved constrained host-network observer, and persisted state. It is not a general `up`/`down` lifecycle implementation and does not expose a public service.
- The host observer is an evidence instrument, not an application or general network-runtime component. It had no mounts, privilege, published port, retained state, or host mutation path; it was owner-checked and removed after each observation.
- The NFQUEUE diagnostic did not bind a queue, install a rule, send a packet, produce a verdict, load a host module, or create a network topology. Native socket creation does not prove target NFQUEUE capability; the static target socket returned `EPROTONOSUPPORT`, and the original dynamic target consumer needs unavailable `GLIBC_2.34`.
- MIPSEL/MIPS have real SHA-256-locked 20-package roots, target GCC-built hello/web-demo/NFQUEUE fixtures, and audited mixed images with 31 target ELFs each. Direct QEMU/PRoot shell/opkg/nested/shebang/fixture diagnostics and bounded Docker/binfmt shell/opkg/nested/fixture argument checks now pass. The first Docker observation failed `exec /bin/sh: exec format error` on both; separate explicit approval added only the MIPS binfmt handlers and verified exact registration. Recheck those handlers after reboot/runner changes. `keemu init` and single-scenario `test` remain AArch64-only; matrix orchestration now exists but reports both MIPS cases BLOCKED, not PASS. A host-side strict NDM process shim has synthetic-only portable proof, without target `/opt/bin/ndmc` installation, observed device bytes or package lifecycle integration; A11 remains BLOCKED. A separate opt-in host-local synthetic boot/netfilter event harness proves only S0 JSON rule-list repeat, not a scenario/device hook or actual firewall restoration; A12 remains BLOCKED. MIPS general lifecycle and network verdicts are not implemented. See `docs/evidence/m1b18-targets.md`, `docs/evidence/m1b19-matrix.md`, ADR-0012 and ADR-0013. One-shot Docker diff is path/change metadata, not a byte-complete filesystem audit.
- Matrix v1 requires an explicit per-case `lock` in addition to the specification's illustrative profile/scenario pair, because the existing runnable lifecycle never accepts an unlocked scenario. Input/schema errors currently exit 2 without an A21 bundle; a valid but incomplete mapping produces a BLOCKED bundle. The aggregate records each child report path and SHA-256; MIPS cases have no child lifecycle report until supported. The matrix `profile`/`runtime` fields are aggregate placeholders, not a fabricated target profile or image provenance; use child reports for actual AArch64 metadata. `--strict` maps WARN-only results to exit 5, but cannot turn required BLOCKED cases into PASS.
- The frozen m1b-22 cross-target acceptance observation (`docs/evidence/m1b22-acceptance.json`) again records A10 BLOCKED: the complete matrix ran an AArch64 child but the MIPS/MIPSEL cases have no lifecycle child. A02–A06/A11 remain BLOCKED on all three; AArch64 live test PASS and synthetic NDM PASS are narrower than whole-ID acceptance. Per-target Docker socket probes returned `EPROTONOSUPPORT` while the explicit native same-namespace controls opened NFNETLINK sockets. This is a non-mutating differential, not proof of NFQUEUE binding or packets; backend/extension, ipset creation, conntrack, forwarding and verdicts were not tested. No host module loading was approved. The shared host is not a fresh Ubuntu VM. Raw evidence is tracked and bound in the ledger; see ADR-0014.
- The later m1c-23 topology is a separate AArch64 Docker routing slice: `br0`, `wan0`, internal LAN/WAN, per-run disjoint subnets, HTTP traversal and router forwarding counters passed. It neither repairs the earlier three-target NFNETLINK blocker nor demonstrates a target NFQUEUE verdict. Router setup required namespace `NET_ADMIN`; client/server need the same capability to install routes. The client has no configured management endpoint/bypass but an adversarial privileged route change was not tested. The worker cannot see the Docker host's unrelated non-Docker routes, so pre-allocation overlap checks combine Docker IPAM and worker-visible routes; Docker may reject an unavailable pool. Docker Engine 29.7.2 accepted the endpoint interface-name driver option; other engines are unverified. m1c-27 later verified explicit owner-checked recovery after network-allocation, first-container and post-setup SIGKILL without affecting a second run's IPv4 policy rule or unrelated Docker resources; automatic recovery, foreign firewall-rule parity and full A18/A20 remain unproven. See ADR-0015, ADR-0017 and `docs/evidence/m1c27-network-interruption.json`.
- m1c-24 added a static target network-demo by compiling the unchanged P0 NFQUEUE consumer into one translation unit with a small API/UI frontend. Native API-only GET/POST/mode-restart tests and a target API-only read smoke passed, but API-only reports `queue_ready=false`, zero real packet counters and `/health` 503. Actual AArch64 queue startup still fails `Protocol not supported`; no rule, packet, verdict, queue counter, host firewall/module mutation or public exposure occurred. The narrow client→server:8080 FORWARD rule hook excludes router control:9090 but has not run against a verified target iptables backend. At the m1c-24 observation target API writes and environment restart were unverified; m1c-25 addresses only their API-only slice below. A14/A17 and MVP 1C remain BLOCKED. See ADR-0016 and tracked `docs/evidence/m1c24-network-demo-raw.json`.
- m1c-25 separately verified target web/API and mode persistence across service and same-container environment restart in diagnostic API-only mode; tracked `docs/evidence/m1c25-web-api.json` and `docs/evidence/m1c25-persistence.json` preserve distinct raw results. The environment restart preserves the writable layer but does not restore/assert router `br0`; client control reaches Docker-provided LAN IP. No packet, rule, verdict, consumer counter increment, kernel counter or functional network backend was proven. A15/A16 remain PARTIAL rather than full PASS; A14/A17 and MVP 1C remain BLOCKED. A brief initial `wget -S` target diagnostic exited 139; final target observations use working `wget -qO-` and real HTTP 400/503 stderr, without claiming the diagnostic was a network result.
- m1c-26 reproduced a blocker on a fresh, fully routed topology: baseline target HTTP and forwarding counter rose 0→10 with no queue; in the same router namespace native NFNETLINK socket opened while target socket and network-demo startup returned `Protocol not supported`. No target queue bind ACK, real NFQUEUE rule, queued packet, ACCEPT/DROP/restored ACCEPT, consumer or kernel queue counters, or pcap is available. The API-only rule hook refused non-ready queue. Tracked `docs/evidence/m1c26-packet-blocker.json` binds raw observations, unchanged absent host NFQUEUE modules and owner-only cleanup. This baseline is not A14/A17 or MVP 1C PASS; module preparation requires operator authorization or a preconfigured capable runner.
- Following explicit approval to load only `nfnetlink_queue`, m1c-26 repeated the same-router differential: native socket/0, static AArch64 socket/1 and real target network-demo startup `Protocol not supported`. `nfnetlink_queue` remained loaded, `xt_NFQUEUE`/`nft_queue` absent, and no new module appeared during the probe. `docs/evidence/m1c26-after-module-queue-recheck.json` freezes the owner-clean BLOCKED result. Loading the approved module did not make target queue creation work; native socket success does not prove native queue binding or isolate QEMU causality. No further host module, rule, packet or verdict test is authorized or claimed.
- Later, explicit bounded native-transport approval allowed an independent static AArch64 target-decision prototype and native x86_64 NFQUEUE transport in a KEEMU-owned router, with provenance in `locks/m1c26-ipc-prototype.json`. Its queue bind ACK and target API readiness handshake were observed, alongside the successful no-rule routed HTTP control. The selected target `/opt/sbin/iptables` does not exist (exit 127), so no exact queue rule or queued packet/verdict was attempted and no queue or rule packet counter/pcap proof exists. `docs/evidence/m1c26-ipc-backend-blocker.json` preserves the raw BLOCKED result. The host `xt_NFQUEUE` extension was loaded by the approved preflight, then safely unloaded after use count 0 when no rule could be installed; `nfnetlink_queue` remains loaded and taint 0. `docs/evidence/m1c26-ipc-backend-rollback.json` records independent postflight. The transport substitution, even if completed later, would not prove direct target NFNETLINK support; A14/A17/MVP 1C remain BLOCKED.
- A further explicit approval provided pinned native `iptables-legacy` userspace in a separate owned image, not in target PATH (`locks/m1c26-native-iptables.json`). Read-only preflight in an owned router found no registered IPv4 legacy table; `iptable_filter` is an unloaded host module with a loadable file. The approval forbids any additional host module, and querying a legacy `filter` table could autoload it, so no firewall rule/list/check or packet proof was attempted. Tracked `docs/evidence/m1c26-iptables-prerequisite-blocker.json` binds the module and rollback observations; `xt_NFQUEUE` was safely unloaded unused, while `nfnetlink_queue` stays loaded. This is another environmental BLOCKED checkpoint, not a claim of target/adapter verdict success.
- Later explicit bounded `iptable_filter` preparation and fresh hash-checked `xt_NFQUEUE` reload allowed the owner-scoped native firewall helper to install the exact rule after adding `NET_RAW` to its existing `NET_ADMIN` capability (the same-router raw-socket differential failed without and passed with `NET_RAW`). The live AArch64 target decision process selected/counted 10 ACCEPT and 7 DROP through the approved native transport; kernel queue sequence, exact-rule counter, adapter verdicts and bounded pcap all observed 17. Routed HTTP ACCEPT→DROP→restored ACCEPT, fail-closed no-listener, exact rule removal and ordered module rollback passed; see `docs/evidence/m1c26-packet-verdicts.json`, `docs/evidence/m1c26-packets.pcap`, `docs/evidence/m1c26-final-rollback.json`. A14/A17 bounded fixture slices are verified under D-038/D-039 substitutions, but direct target AArch64 NFNETLINK still returns `Protocol not supported`, `/opt/sbin/iptables` remains absent, and no generic package or physical device path is proven. Target API `queue_ready` remains true after adapter death, although kernel queue entry disappears and routed data fails closed; do not treat the stale API field as live-listener proof. Full MVP 1C is not complete.
- This worker namespace cannot read host `/proc/sys/fs/binfmt_misc`; both MIPS `keemu doctor` profiles still report binfmt BLOCKED/exit 4 despite separately verified host entry readback and passing actual target Docker execution. Do not infer doctor PASS from the runtime probe or Docker failure from the doctor visibility gap.
- Common `RunReport` schema-version-2 metadata and immutable bundle primitives exist, but most later runtime producers do not yet populate full lifecycle provenance.
- Input schemas and loaders prevent ambiguous keys, unsupported versions, unowned path traversal, and symlinked inputs at validation time. The one-shot runner uses no-follow input reads, rehashes staged bytes and verifies the Docker copy-back; other consumers still need use-time revalidation. The scenario lock loader checks local source/scenario hashes, while the runner checks profile/feed/base/image bindings; independent live QEMU/binfmt provenance is incomplete. Only exact `127.0.0.1` host binding is representable in the version-1 scenario, and the one-shot runner declines publishing. Public exposure requires separate approval.
- The m1a-11 `init` cache covers the locked AArch64 base only, not fixture installation or full cross-target A01/A19 acceptance. It requires staged local P0 artifacts plus a live Docker/binfmt runner for its first build; offline repeat requires the Docker-local image to remain present. The pinned public feed URL in `/opt/etc/opkg.conf` identifies origin but is not a content-addressed upstream snapshot: `init` never invokes `opkg update`, and one-shot `test` installs only a hash-verified local IPK. Installed-time fields are normalized to zero for repeatable bytes; Docker COPY removes BusyBox's setuid bit, explicitly recorded in the image audit. The recorded `oci_digest` is the local Docker image ID, not a registry manifest digest. Target DNS passed inside the bridge container; HTTPS checked only from the Hermes process namespace with default CA/hostname validation because pinned BusyBox wget lacks TLS. Do not misreport this as target HTTPS. Cache input pathname race hardening for untrusted writable directories and full interruption recovery remain later work.
- The m1a-13 `test` runner uses a disposable target BusyBox gzip link because the immutable base lacks one. It compares Docker diff path metadata after removal with explicit residual allowances; it does not prove file-content identity, full socket/process closure, or cleanup after process crash/SIGKILL. Real failures retain the primary stage and a separately assessed cleanup status. Host publish, target HTTPS/UDP and other architectures are not covered.
- The m1a-14 persistent path pins a local AArch64 IPK, records owner-checked Docker identity before install, and refuses replacing names, input snapshots or Docker IDs. It accepts only target-loopback HTTP readiness without host publication or added capabilities. It does not run all scenario checks, expose an extra-package `install` command, or create the one-shot runner's JSON/Markdown/JSONL bundle for persistent failures. An m1a-16 real SIGKILL test verified explicit owner-only recovery/retry at the pre-ID and post-start stages, but this requires a separate `recover --yes` invocation: normal CLI start does not auto-repair. It discards uncertain installation/service state rather than replaying postinst; it does not emit a schema-version-2 partial report, cover one-shot SIGKILL, automatic network-resource recovery, an untrusted same-UID actor, a full socket census or a hard disk quota. A same-UID adversary racing the owner-only registry path is outside the cooperative flock guarantee; m1c-27's separate explicit topology recovery does not change that persistent limitation.
- The m1a-15 AArch64 TLS frontend is a separate fixture component in an immutable P0 web-demo image; it does not turn generic `keemu test`/`up` into a host-published HTTPS/UDP lifecycle. A disposable, bounded host-network observer tests only Docker-host loopback; the target never joins the host network. TLS trust exists only in that observer's local CA context, not target BusyBox or the host's global store. SDK archives are SHA-256-pinned local build inputs; an offline repeat needs those archives and the existing Docker-local images. Ephemeral host ports can change after Docker restart and must be read back. No private keys are tracked. General persistent report bundles and full A02–A09/A19 acceptance remain unproven.
- PRoot is not a security or container-isolation boundary. A generic AArch64 result does not establish compatibility with a Keenetic model or KeeneticOS release.

## Truthfulness rule

The m1a-17 ledger in `docs/evidence/m1a17-acceptance.json` freezes a project-clean but shared-host AArch64 Docker/binfmt run, not a clean-Ubuntu installation. All A01–A09 and A18–A21 whole IDs were BLOCKED **at that observation** for listed gaps even though their AArch64 slices passed. MIPS/MIPSEL and fresh-Ubuntu were SKIP in that historical ledger; m1b-18 first recorded Docker/binfmt BLOCKED and subsequently added separately authorized MIPS handlers, producing new hash-bound PASS evidence on both targets. Together with the prior AArch64 proof, the A01 three-generic-target ELF/shell/nested-exec check is now PASS; this is not retroactive modification of the m1a-17 ledger. The earlier ignored MIPS failure probe bytes were overwritten by the approved host remediation and are unavailable; the historical ledger digest is retained, while current PASS raw bytes are tracked in Git. Other ignored raw JUnit/reports require local retention to recheck their SHA-256. Wrong-architecture and shebang package reports correctly FAIL at inspection. No network acceptance is inferred.

P0 completion means its four technical-risk conditions have evidence or an explicit reproducible blocker. It is not a claim that every acceptance ID is PASS, that NFQUEUE ACCEPT/DROP works, or that MVP 1 is complete. Missing checks remain NOT RUN, PARTIAL, or BLOCKED as shown in `docs/traceability.md`.
