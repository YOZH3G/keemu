# MVP 1A frozen acceptance observation (m1a-17)

2026-09-24, branch `agent/keemu`, Docker Engine 29.7.2 / Linux 6.8.0-138-generic. The runner had no KEEMU-owned container or network before the live run and none after. It is a shared Docker host with unrelated Hermes/Traefik containers, **not** a fresh Ubuntu VM. The recovery probe independently matched their container/network identity and state before/after; target AArch64 shell/opkg/nested ELF executed in Docker, while the worker namespace cannot read the host binfmt entry.

Authoritative frozen ledger: `docs/evidence/m1a17-acceptance.json` SHA-256 `b8c6fea25231dc5fbe4f30b3607fc9d289d3f5deef706e44369d19ac27b829be`. It enumerates all 13 applicable IDs (A01–A09, A18–A21), exact test names/outcomes, per-ID blockers, negative-package FAIL observations, explicit phase SKIPs, runner scope and SHA-256-bound ignored reports. The generator `tests/integration/freeze_m1a17.py` verifies inputs and refuses overwrite. Unit checks validate ledger completeness and locally retained artifact hashes.

- Opt-in Docker/binfmt bundle: `KEEMU_RUN_INIT_CACHE=1 KEEMU_TEST_DOCKER_RUNTIME=1 KEEMU_TEST_LIFECYCLE=1 KEEMU_TEST_PERSISTENT=1 KEEMU_TEST_RECOVERY=1 KEEMU_M1A15_LIVE=1 uv run pytest -q --junitxml=.runtime/m1a17-live-junit.xml tests/integration/test_init_cache.py tests/integration/test_docker_runtime.py tests/integration/test_lifecycle.py tests/integration/test_m1a15_https.py tests/integration/test_m1a16_recovery.py` → **22 PASS, 0 FAIL, 0 SKIP**. Raw JUnit SHA-256 recorded in ledger.
- Default portable suite `uv run pytest -q --junitxml=.runtime/m1a17-portable-junit.xml` → **130 PASS, 0 FAIL, 19 SKIP** (opt-in Docker cases). Raw JUnit SHA-256 recorded in ledger. Ledger-only unit tests added afterward: 2 PASS; see final verification for the current total.
- Negative package observations: wrong architecture and bad shebang yielded two real **FAIL** one-shot reports at `inspect`; both are expected negative self-test outcomes, not failed harness tests. Failed postinst/startup, timed-out command and injected cleanup-error reports retain distinct primary/cleanup semantics. These are not incorrectly promoted to package PASS.
- **SKIP**: MIPS/MIPSEL acceptance belongs to MVP 1B; the fresh-Ubuntu instruction was not run on this shared host. Both remain explicit requirements, not implicit PASS.
- **BLOCKED**: all 13 whole acceptance IDs and MVP 1A gate. Scoped AArch64 fixture/runtime checks PASS, but cross-target A01–A06, generic published persistent HTTP/HTTPS/UDP, complete recovery/isolation/report provenance, and clean-host validation are not proved. Exact per-ID reason in the JSON ledger. This frozen subtask does not implement later MVP 1B/1C work.

Runtime evidence and JUnit XML live under ignored `.runtime/` and `reports/`; retain them to verify the manifest hashes. The committed JSON captures test names/statuses and hashes, but not the raw ignored artifact bytes on another checkout. No new host binfmt, firewall, kernel module, public port or foreign-resource mutation was made.
