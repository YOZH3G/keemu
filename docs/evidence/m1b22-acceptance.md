# m1b-22 three-target acceptance observation — BLOCKED

On 2026-09-24, the shared Docker Engine 29.7.2 host ran the opt-in AArch64 lifecycle, recovery, fixture and matrix suites plus offline MIPS/MIPSEL image/inventory tests: **28 PASS, 0 FAIL, 0 SKIP**. The pre-ledger portable suite: **187 PASS, 0 FAIL, 22 opt-in SKIP**. Frozen, no-replace ledger `docs/evidence/m1b22-acceptance.json` has SHA-256 `cf0dca2c0775fed8b055dd6454ecf4195957eb8dd9048d83751b10509d481841`. Its raw live/portable JUnit, matrix parent/child, three doctor results and capability probe are retained as `docs/evidence/m1b22-*` and SHA-256-bound to their ignored original run paths. A later full portable suite includes separate evidence-validator tests and must not be confused with the frozen pre-ledger counts.

A01: PASS only for specified three generic targets' ELF/shell/nested-exec checks; the prior m1a-17 and approved m1b-18 target ledgers remain unchanged. A02–A06: BLOCKED as whole IDs on every target. AArch64 one-shot/persistent positive slices, wrong-architecture and bad-shebang negative reports, failure/cleanup cases passed; negative package reports are **FAIL at inspect**, while the self-tests expecting FAIL are PASS. File-byte equality, independent postinst exit, full socket/process census, all end-to-end negative variants, persistent failure reports, and MIPS/MIPSEL general `keemu init/test` remain unproved. No installed-package lifecycle was run on MIPS/MIPSEL.

A10: complete three-profile matrix mapping, real AArch64 child PASS, required MIPSEL/MIPS static BLOCKED, aggregate BLOCKED. Parent SHA-256 `8c20c0a4a62655f4e28392bc42a0e41d4d249df1989b5809f64bd8df59545189` binds AArch64 child SHA-256 `a7afdd55e9d5ebee7d6d1f779326b9c5e93a12190630d032c80247148bfcf5f5`; neither MIPS case has a child lifecycle report. Portable architecture mismatch returns package FAIL (not environment BLOCKED). A11: synthetic host Python shim subprocess tests passed exact known/unknown contract behavior, but no target-installed `ndmc`, observed physical response, or package invocation exists. A11 remains BLOCKED on all three. MVP 1B remains BLOCKED; A12's separate synthetic S0 status also remains BLOCKED.

Per-target, owner-labeled, network-none, cap-drop-all, read-only-root containers executed a compiled target NFNETLINK socket probe and an explicit native amd64 control within each same namespace. Raw `docs/evidence/m1b22-capabilities.json` (SHA-256 `8145f33913581e566935a48e4f2bf8ec55dd596e232db4fabdc9140d370ee8f8`): native control opened the socket (exit 0) on all three; target AArch64, MIPSEL, MIPS each returned exit 1, `NETLINK_NETFILTER socket: Protocol not supported`. This does not isolate QEMU translation from seccomp/kernel causality. All three target rootfs probes searched tool availability without executing an iptables backend. iptables extensions, ipset creation/protocol, conntrack, forwarding, raw socket needs, queue binding, packet decoding and verdict are BLOCKED/NOT RUN, never PASS. No host module, rule, queue, firewall, packet, public port or host network was changed. The observed NFQUEUE module snapshot was absent/identical before and after. Each container was full-ID/owner/run-label checked, stopped/removed, and independently absent. Final owner-label container/network queries were empty.

`keemu doctor` per profile returned BLOCKED/exit 4 solely on binfmt visibility in this worker's `/proc` namespace. QEMU CLI, Docker daemon, platform and disk probes passed. This does not revoke prior actual Docker target execution; conversely Docker execution does not make doctor PASS. This was a project-clean shared host, not a fresh Ubuntu VM. Earlier failed diagnostic attempts encountered Docker-inspect output truncation, Docker `cp` refusal on read-only root, and a missing shell `cat` applet; each residual owned container was checked and removed, and the corrected test streamed bytes via explicit target BusyBox into bounded tmpfs. Those attempts are not acceptance PASS. No network-demo or MVP 1C work was started.
