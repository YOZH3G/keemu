# m1b-18 MIPS/MIPSEL target observations (historical BLOCKED → PASS)

The immutable initial ledger is `docs/evidence/m1b18-targets.json` (SHA-256 `816727a7b0ea632946cbca3dcad6a2f75b286df7916f6705c34a9b16c8ea8f0c`). Its original ignored raw probe `.runtime/m1b18/probe.json` had SHA-256 `2d57090222f0b1b96a54cbf131b2f4ce0a49ac48becba2f21e5aea69a43a60b7`; those bytes are no longer available (see follow-up below).

- Two independent 20-IPK Entware closures from `mipselsf-k3.4` and `mipssf-k3.4` and their feed indexes/bootstrap opkg are SHA-256 locked; separate 16-package per-target GCC SDK closures produced hello, web-demo and NFQUEUE-consumer fixtures. The live feed URLs are mutable, so only cached exact bytes are repeatable.
- Static audit of each mixed `linux/amd64` image found 31 MIPS ELF32 target files and exactly one native amd64 init. All target ELFs report o32, MIPS32r2 and soft float; endian differs as expected (MIPSEL little, MIPS big). Saved single-layer archives were compared file-for-file to staged rootfs, allowing only Docker's verified BusyBox setuid-bit attenuation. A second offline image rebuild reproduced both image IDs and archive hashes exactly.
- Direct QEMU 10.0.13/PRoot diagnostic on each target passed real Entware shell, bootstrap and installed `opkg`, exact 20-package inventory, nested target BusyBox, target shebang, hello output, and web-demo/NFQUEUE argument handling. This path is not Docker acceptance or packet processing.
- Bounded project-owned `linux/amd64` Docker containers (network none, read-only, cap-drop-all, CPU/memory/PID bounds, no host mount or published port) started native init but target `docker exec /bin/sh` returned `exec /bin/sh: exec format error` for both. Both full-ID/run-label-verified containers were stopped and removed; independent queries found no residual project resource. Read-only `keemu doctor` with the staged QEMU in PATH returned exit 4/BLOCKED for binfmt for both profiles; this worker namespace cannot directly inspect the host binfmt entry. No registration was changed.

At this initial observation A01 three-target acceptance was BLOCKED. A10 matrix validation/execution belongs to m1b-19 and was NOT RUN; two per-target artifacts are not a matrix PASS. User approval was required before host `qemu-mipsel`/`qemu-mips` binfmt registration; it was subsequently granted and applied in a separate bounded operation. `keemu init` and `keemu test` still support only AArch64; these new targets are locked preparation/probe paths, not general CLI lifecycle support.

Local repeat: `uv run python -m scripts.verify_m1b18` (no fetch/build); `KEEMU_RUN_M1B18=1 uv run pytest -q tests/unit/test_m1b18_targets.py` (offline image integrity). Full rebuild from cached captured inputs: `uv run python -m scripts.prepare_m1b18_roots` (only when roots absent), `uv run python -m scripts.build_m1b18_fixtures`, `uv run python -m scripts.build_m1b18_images`. Rebuild commands refuse divergent published locks. The capture script `scripts.capture_m1b18` is online-only initial preparation and refuses to replace an existing lock; do not rerun against mutable feed expecting the same snapshot.

## Approved host capability follow-up (September 24, 2026)

The paragraph above records the **historical** pre-approval observation only. The approved host action added exactly `qemu-mips` and `qemu-mipsel` from the pinned QEMU package; exact binfmt magic, mask, interpreter and POF flags were read back, while `qemu-aarch64` stayed unchanged. The readback is retained under `/opt/data/backups/keemu-stack-20260922T073131Z/binfmt-mips-approval-20260924T192130Z/postflight.json`. That host change was separate from the unprivileged project probe.

No-replace follow-up ledger `docs/evidence/m1b18-targets-pass.json` SHA-256 `ca7b0741c09997ffb606077422503198cfde3245c16d9db1a2a24f495fe03f70` binds the tracked `docs/evidence/m1b18-probe-pass.json` SHA-256 `b17b30fed1e00c71e681df01dbd7d64c4998358a7be68e244d06826aded7a9ed`, all eight MIPS locks, the older BLOCKED ledger and the m1a-17 AArch64 ledger. Per target: 20 installed IPKs, 31 target ELF32/o32/MIPS32r2/soft-float files, opposite expected endianness, offline artifact/image audit PASS, direct shell/opkg/inventory/nested/shebang/hello/web-demo+NFQUEUE argument probes PASS, and bounded Docker target shell/opkg/nested/hello/web-demo+NFQUEUE argument probes PASS. Owner-checked cleanup and project-label absence PASS. With the prior AArch64 generic proof, A01's three-target ELF/shell/nested-exec check is PASS. A10 matrix remains NOT RUN (m1b-19); MIPS `keemu init`/`test`, other lifecycle checks, strict NDM and full MVP 1B remain unfinished.

This worker cannot read host `/proc/sys/fs/binfmt_misc` entries. With QEMU in PATH, `keemu doctor --profile generic-mipsel` and `generic-mips` therefore still return exit 4/BLOCKED for the binfmt check despite independently passing Docker target execution and the separately captured exact host readback. Do not report `doctor` as PASS; this is a namespace-observability limitation, not a failed Docker execution.

The historical ledger's raw probe path was reused by the separately authorized host correction, so the original ignored raw bytes are **unavailable**, though its recorded SHA-256 and committed BLOCKED ledger remain unchanged. Current PASS raw bytes are committed so future probes cannot silently replace them. Re-run the bounded differential with `uv run python -m scripts.probe_m1b18` (writes a unique ignored path) and offline integrity with `uv run python -m scripts.verify_m1b18`; a new ledger must not overwrite either frozen observation.
