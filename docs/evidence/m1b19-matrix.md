# m1b-19 matrix observation (A10 BLOCKED)

On 2026-09-24, `KEEMU_TEST_MATRIX=1 uv run pytest -q tests/integration/test_matrix.py` passed one live opt-in Docker/binfmt test. Its input generator assembled three separate locked hello IPKs/scenarios under ignored `.runtime/`, bound the AArch64 case to the verified `locks/m1a-init-aarch64.json` Docker-local image and feed hash, and requested all three generic targets in order. The matrix report is `reports/matrix-b84ba904a6bd44a7a84039f4148216b7/report.json`, SHA-256 `1543ed6d4144f9af3877fe5ba28b1e72b63683f7a4c36f701c09faa6332f05e3` (ignored local artifact). Its AArch64 child report is `reports/matrix-b84ba904a6bd44a7a84039f4148216b7-case-1/report.json`, SHA-256 `6a65e995f5d18b890898cc2668995d579e2622ee6015009a920b6529d55528ad`. The parent binds child path/hash and retains its matrix source bytes; the child retains validated scenario/lock bytes. Generated input directories were deleted at test completion, so the retained reports and test generator, not the temporary IPK files, are the reproducible observation.

Observed required checks: completeness PASS (three distinct profiles), generic-aarch64 PASS (real target opkg lifecycle and owner cleanup), generic-mipsel BLOCKED (static preflight; no general IPK lifecycle), generic-mips BLOCKED (same). Overall BLOCKED/exit 4; no MIPS case was silently skipped or asserted as executed. A01's separate three-target shell/ELF probes remain valid, but do not supply A10 installed-package PASS. Docker Engine 29.7.2 was reachable. Read-only owner-label queries found no project-owned container/network after the run; no host binfmt, firewall, module or public port change occurred.

After a report-cause/mode correction and added child-error precedence regression, the live opt-in test passed again. Latest parent `reports/matrix-977fbb17ec7f496cb4d8802e10fd5b2f/report.json` SHA-256 `965c87af8a01a8b6345c9db68ad51b18b628b9ecc38e5f324dc4e07d6276d14e` has real AArch64 PASS, required MIPS cases static/environment BLOCKED and aggregate BLOCKED. Latest child `reports/matrix-977fbb17ec7f496cb4d8802e10fd5b2f-case-1/report.json` SHA-256 `c8f728d9e715be64037951dad10b3fc7b7c8f3aebe3f68a47e6d09b390bbb5a1` is PASS. The earlier reports remain unchanged historical observations.

Final portable suite: `uv run pytest -q` returned 153 passed, 22 opt-in skipped. The enabled matrix integration test separately returned 1 passed. Unit cases cover duplicate/unknown versions and keys, unsafe paths and hash changes before any Docker call, incomplete mapping BLOCKED/exit 4 even with `--strict`, unsupported target BLOCKED, architecture-mismatch FAIL/exit 1 overriding required BLOCKED, child ERROR overriding BLOCKED, immutable parent report collision, source-JSON/Markdown parity, and ordered case visits. `uv run ruff check .`, touched-file formatter and `git diff --check` passed. Final Docker owner-label queries returned no container or network.

Scope: general MIPS/MIPSEL `keemu test` still unsupported. No three-target IPK matrix PASS, NDM shim, event contract, or MVP 1B gate is inferred. See ADR-0011 and `docs/limitations.md`.
