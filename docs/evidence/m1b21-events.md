# m1b-21 synthetic event observation

Scope: host-local synthetic fixture only; no Docker, target hook, kernel firewall, or physical Keenetic response.

- `uv run pytest -q tests/unit/test_events.py`: 19 passed. Real Python subprocess invocations of `python -m keemu.events` test synthetic boot and netfilter, exact digest-pinned handler order, cwd, PATH and event environment, nonexecutable skip/fail, stop/continue, timeout, redacted JSONL, unknown-event BLOCKED/4, invalid-input exit 3, script failure/incorrect state FAIL/1 and repeat restoration without duplicate JSON rule identifiers.
- `uv run pytest -q`: 187 passed, 22 opt-in skipped. `uv run ruff check .`, `uv run ruff format --check src/keemu/events.py tests/unit/test_events.py` and `git diff --check` passed.
- `tests/unit/test_events.py` creates all scripts, fake chain/rules and state under pytest-owned temporary directories; no host firewall command, network packet, Docker container, real target binary or privileged action occurs. This proves only S0 rules-mock repeat. A12 whole acceptance and MVP 1B stay BLOCKED pending a confirmed scenario event contract plus isolated real firewall repeat proof. No A21 run-report bundle is generated by this standalone harness.
