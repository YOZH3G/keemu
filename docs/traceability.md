# Acceptance traceability

Most rows retain pre-m1a-17 slice statuses; A14/A17 reflect the later bounded m1c-26 fixture. `PARTIAL` is not PASS; the frozen m1a-17 ledger below is authoritative **at its capture time**. The later m1b-18 PASS follow-up closes the A01 three-generic-target execution gap without rewriting either earlier observation. The m1b-22 frozen three-target observation below supersedes stale A02–A06/A10/A11 table readings, without changing prior ledgers.

| ID | Prior slice status | Test or scenario | Latest evidence | Gap |
|---|---|---|---|---|
| A01 | PARTIAL (historical table; now PASS for specified three generic target ELF/shell/nested exec) | `tests/integration/p0_runtime_probe.py`; `tests/integration/test_init_cache.py`; `scripts/verify_m1b18.py`; `scripts/probe_m1b18.py` | AArch64 Docker/binfmt PASS in m1a-17. MIPSEL/MIPS initially BLOCKED (`docs/evidence/m1b18-targets.json`), then separately approved binfmt plus `docs/evidence/m1b18-targets-pass.json` proved little/big ELF32/o32/MIPS32r2/soft float and real target shell, opkg, nested ELF/hello and fixture arguments inside bounded Docker; direct shebang also PASS. | A01's specified target-exec check is closed. Base `init` and general lifecycle remain AArch64-only; A02–A06/A10 and full MVP 1B remain open |
| A02 | PARTIAL | `tests/integration/test_lifecycle.py`; `tests/integration/test_m1a15_https.py`; `tests/unit/test_ipk_inspect.py` | m1a-13 live AArch64 synthetic hello IPK installed and removed with real target opkg; m1a-15 repeated exact locked scenario/IPK bytes twice offline with postinst count one, executable, package inventory, removal, reports and clean owner-only cleanup. | No independent postinst exit code, installed-file byte equality, or MIPS/MIPSEL result; no full A02 PASS |
| A03 | PARTIAL | `tests/integration/test_lifecycle.py`; `tests/unit/test_registry.py` | Locked static AArch64 web-demo embedded in a test IPK: one-shot target-loopback readiness, explicit stop, refused connection, opkg remove and bounded residual comparison passed. m1a-14 persistent web-demo readiness passed across same-container restart/down/up; hello retained installed state across down/up. | No process-lineage census, full socket inventory, general host publish or cross-target run |
| A04 | PARTIAL | `tests/unit/test_ipk_inspect.py`; `tests/integration/test_lifecycle.py` | Wrong-architecture package reaches one-shot static `inspect` and yields a report FAIL before Docker; persistent create rejects the same static defect before registry/Docker allocation. ELF/loader/dependency cases exist in inspector unit tests | End-to-end wrong ELF/loader/dependency lifecycle fixtures and other targets not run |
| A05 | PARTIAL | `tests/unit/test_ipk_inspect.py`; `tests/integration/test_lifecycle.py` | Unsafe shebang reaches one-shot static `inspect` and yields a report FAIL; persistent create rejects unsafe shebang before registry/Docker allocation. Permission and link variants are covered by inspector unit tests | End-to-end permission/broken-link lifecycle fixtures and other targets not run |
| A06 | PARTIAL | `tests/integration/test_lifecycle.py` | One-shot failed postinst/startup and timeout retain JSON/Markdown/JSONL plus cleanup evidence. Persistent failed postinst retains `failed` registry state and verifies owner-only container absence. | Persistent CLI errors do not yet publish a full report bundle; further fault injection, crash/SIGKILL recovery and cross-target tests not run |
| A07 | PARTIAL (HTTP/HTTPS AArch64 fixture slice PASS) | `tests/integration/p0_web_demo_probe.py`; `tests/integration/test_m1a15_https.py` | P0 HTTP publish PASS retained. m1a-15 static AArch64 HTTPS frontend, ephemeral trusted local CA, negative wrong-name/untrusted-CA checks and explicit Docker-host-loopback HTTP/HTTPS probe passed on exact localhost-only publishes; `.runtime/m1a15/live-m1a15-0303315534cc/evidence.json` SHA-256 `6758497e7db513891b6f2f9ae089518903293edda6793cee50172e625d8843a3`. | HTTPS is a separate fixture component; general `keemu test`/`up` host publish and HTTPS probes remain unsupported |
| A08 | PARTIAL (AArch64 fixture slice PASS) | `tests/integration/p0_web_demo_probe.py`; `tests/integration/test_m1a15_https.py` | UDP echo from Docker-host loopback passed before/after service and container restart through inspected explicit `127.0.0.1` publish; same m1a-15 evidence. | General scenario lifecycle host UDP probe remains unsupported |
| A09 | PARTIAL (AArch64 fixture slice PASS) | `tests/integration/p0_web_demo_probe.py`; `tests/integration/test_lifecycle.py`; `tests/integration/test_m1a15_https.py` | HTTPS API state write `secure` persisted across same-container service restart and Docker stop/start with HTTP/HTTPS/UDP host rechecks; same m1a-15 evidence. m1a-14 retained hello postinst count across persistent down/up. | Fixture probe bypasses general persistent registry; generic published persistent lifecycle unverified |
| A10 | BLOCKED (m1b-19 matrix observation; not PASS) | `tests/unit/test_matrix.py`; `tests/integration/test_matrix.py`; `schemas/matrix.schema.json` | `docs/evidence/m1b19-matrix.md` binds one real AArch64 child PASS and required MIPSEL/MIPS case BLOCKED results. Strict schema, completeness, input locks, architecture mismatch FAIL, unsupported-target BLOCKED and aggregate precedence are tested | MIPS/MIPSEL `keemu test` lifecycle remains unsupported. No three-target package installation matrix or full A10 PASS; m1b-22 cross-target acceptance still pending |
| A11 | BLOCKED (portable synthetic process-contract slice PASS) | `tests/unit/test_ndm.py`; `src/keemu/ndm.py` | Actual Python subprocess tests cover exact `ndmc -c` argv/stdout/stderr/exit, stateful read after write, no-effect failure, unknown-call exit 64 and redacted JSONL; `required_check` maps required unknown to environment-gap BLOCKED. See ADR-0012. | No observed physical-device fixture, target `/opt/bin/ndmc` install, real package invocation or general lifecycle integration; no full A11 PASS |
| A12 | BLOCKED (synthetic rules-mock repeat PASS only) | `tests/unit/test_events.py`; `src/keemu/events.py` | Host-local Python subprocess tests dispatch SHA-256-pinned synthetic boot and netfilter handlers in filename order; explicit cwd/PATH/type/table, nonexecutable policy, timeout, stop/continue, redacted JSONL, and three successive states (initial, test-owned drift, restored) verify exact unique-rule restoration. See ADR-0013. | No observed device contract, scenario/persistent CLI integration, target hook, actual firewall chains/rules or real netfilter event. S0 rules-mock cannot satisfy A12's firewall repeat acceptance; no host firewall mutation was attempted |
| A13 | PARTIAL (isolated AArch64 routing slice PASS) | `src/keemu/topology.py`; `tests/integration/test_m1c23_topology.py`; `tests/integration/test_m1c26_ipc.py` | Two internal Docker networks; client only LAN, server only WAN, router `br0`/`wan0`, default/reverse routes and forwarding. Target web-demo HTTP crossed the router (m1c-23); later m1c-26 evidence confirms the same routed flow through queue-42 verdicts under approved substitutions. `docs/evidence/m1c23-topology.json` SHA-256 `9e8ce09bc0e8f0bc4689af5a03bac36393e58d17309b92b394c3cec30787dfe0`. | No generic scenario lifecycle or hostile NET_ADMIN route-change isolation proof. Docker-host non-Docker routes are not visible to this worker; see ADR-0015. |
| A14 | PASS (bounded AArch64 decision fixture via approved native NFQUEUE transport; direct target socket BLOCKED) | `tests/integration/test_m1c26_ipc.py` | `docs/evidence/m1c26-packet-verdicts.json` SHA-256 `4098769467cd0665b74b31f32d1bd46e1598ba26eb903cfef022705408051708`: routed ACCEPT→DROP→restored ACCEPT; target 10 ACCEPT/7 DROP, native adapter 17 verdicts, kernel queue sequence 17, exact-rule counter 17, bounded pcap 17 packets. The earlier direct target socket failures remain in `docs/evidence/m1c26-after-module-queue-recheck.json`. | D-038 transport and D-039 pinned firewall installer are explicit substitutions; neither proves direct AArch64 NETLINK_NETFILTER or target iptables compatibility. Not a generic package/device result. |
| A15 | PARTIAL (target API-only web/API slice PASS) | `tests/unit/test_network_demo.py`; `tests/integration/test_m1c25_network_demo.py`; `tests/integration/test_m1c26_ipc.py` | Real AArch64 client GET UI/state and POST accept/drop/readback, invalid POST HTTP 400 and health HTTP 503; later m1c-26 target API mode changes drive real queued decisions via approved native transport. `docs/evidence/m1c25-web-api.json` SHA-256 `150b32bee85ae0621576fdd29be639b4d1e71b986941d3da5528e6ed8b9ecab4` | API-only prior observation lacks a working backend; m1c-26 is a diagnostic substituted fixture, not a general production service. Full A15 and MVP 1C remain open. |
| A16 | PARTIAL (target API-only persistence slice PASS) | `tests/integration/test_m1c25_network_demo.py`; `src/keemu/network_demo.py` | Target POST selected `drop` and mode file `drop\n` survived actual service stop/start and same-ID Docker router stop/start with client GET readback. Separate raw `docs/evidence/m1c25-persistence.json` SHA-256 `1728e3977fa3c0ead36c5294d5be3db7683f78dfcfe0d1f540b7e66e21b5eab4` | Router `br0` was not restored/asserted after container restart; m1c-26 proves live mode effect but not queued decisions after a restart or through generic persistent CLI. A16 whole-ID remains PARTIAL. |
| A17 | PASS (bounded network-demo packet fixture via approved substitutions) | `tests/integration/test_m1c26_ipc.py`; `src/keemu/network_demo.py` | Exact `/32` client→server TCP:8080 FORWARD queue-42 rule without bypass observed: target mode API switched accept/drop/accept and routed HTTP succeeded/failed/succeeded; no-listener queue disappeared and HTTP failed closed while rule counter advanced 17→25 without further target decisions; rule removal restored routed HTTP. `docs/evidence/m1c26-packet-verdicts.json` and `docs/evidence/m1c26-packets.pcap` (SHA-256 `e8081be081e6b068b02eed7f02a11a0bc1d97fb730d3cebb390e741c1677c445`). | Target API `queue_ready` remains stale after adapter death despite kernel listener disappearance; no generic package lifecycle or direct target queue socket proven. |
| A18 | PARTIAL | `tests/integration/p0_runtime_probe.py`; `tests/unit/test_registry.py`; `tests/unit/test_recovery.py`; `tests/integration/test_m1a16_recovery.py`; `tests/integration/test_lifecycle.py`; `tests/integration/test_m1c23_topology.py` | Prior m1a-12–16 identity/recovery evidence retained. m1c-23 live-tested owner/run-ID-checked removal of three topology containers and two internal networks, idempotent cleanup, simultaneous foreign-run preservation and automatic rollback on injected failure after network allocation. Final Docker container/network inventories matched the foreign pre-run IDs; see ADR-0015. | Topology recovery is explicit, not automatic; no real SIGKILL at a network-allocation boundary, cross-target topology, A21 interrupted report or complete one-shot crash journal. Full A18 remains unproven. |
| A19 | PARTIAL | `keemu init --profile generic-aarch64 --locked [--offline]`; `tests/integration/test_init_cache.py`; `tests/integration/test_m1a15_https.py`; `locks/m1a15-https-frontend-aarch64.json` | Existing locked base/cache evidence retained. m1a-15 offline-only SDK recipe rebuilt the static TLS binary to SHA-256 `857719c919b01eb1091c83ef31a620ec06e0849d51ce7e56ced449cd95b3b4f0`; two real hello one-shot reports from identical scenario/IPK hashes passed with offline init and no fetch (`reports/m1a15-hello-a87c53187c26/report.json`, `reports/m1a15-hello-145ba7fe0533/report.json`). | Prepared local SDK/base/image still required; general published fixture install/up repeat and other architectures remain unimplemented |
| A20 | PARTIAL | `tests/unit/test_p0_image.py`; P0-05/P0-06 probes; `tests/integration/test_docker_runtime.py`; `tests/integration/test_m1a16_recovery.py`; `tests/integration/test_m1c23_topology.py` | Earlier zero-cap persistent proof remains unchanged. m1c-23 topology uses only explicit `NET_ADMIN` inside isolated namespaces, with `--cap-drop ALL`, nonprivileged containers, internal bridges, no management/host network attachment, binds, volumes, socket or published ports; inspect readback and foreign resource preservation passed. No `SYS_MODULE` or host module operation. | NET_ADMIN is intentionally added for network setup, unlike the earlier zero-cap runtime. No hard/continuous writable-layer disk quota, complete socket/process/LSM audit or hostile NET_ADMIN bypass proof; full A20 not proven. |
| A21 | PARTIAL | `tests/unit/test_reports.py`; `tests/unit/test_lifecycle.py`; `tests/integration/test_lifecycle.py` | Schema-version-2 JSON remains the Markdown source. m1a-13 write-once bundles retain exact scenario/lock bytes, per-operation bounded stdout/stderr, exit/timeout metadata, optional Docker metadata-only diff, substitutions, coverage, primary failure and cleanup status even for failed runs. | Full filesystem content snapshots, complete live QEMU/binfmt/Git provenance, cross-target/network evidence and recovery metadata remain absent |

## MVP 1A frozen acceptance bundle (m1a-17)

`docs/evidence/m1a17-acceptance.json` (SHA-256 `b8c6fea25231dc5fbe4f30b3607fc9d289d3f5deef706e44369d19ac27b829be`) records 22 PASS/0 FAIL/0 SKIP opt-in live Docker/binfmt tests and 130 PASS/0 FAIL/19 SKIP portable tests. Its per-ID results for A01–A09 and A18–A21 are all **BLOCKED as whole acceptance IDs**; each has a bounded AArch64 PASS slice and an exact outstanding requirement. Two deliberate defective-package reports are **FAIL at inspect**, with PASS harness self-tests. MIPS/MIPSEL and fresh-Ubuntu checks are explicit **SKIP**, not inferred PASS. The shared Docker host was clean of KEEMU-owned resources, but was not a fresh Ubuntu runner; its foreign resources were verified unchanged. MVP 1A gate is BLOCKED. This ledger supersedes any reading of the earlier `PARTIAL` table as a whole-ID PASS, without erasing the earlier evidence or implementing future targets.

## MVP 1B m1b-18 bounded MIPS/MIPSEL observation

The initial `docs/evidence/m1b18-targets.json` (SHA-256 `816727a7b0ea632946cbca3dcad6a2f75b286df7916f6705c34a9b16c8ea8f0c`) preserves the pre-approval Docker `exec format error`. Its ignored raw probe was later overwritten by the approved capability check and is unavailable; its digest remains in the immutable ledger. The follow-up `docs/evidence/m1b18-targets-pass.json` (SHA-256 `ca7b0741c09997ffb606077422503198cfde3245c16d9db1a2a24f495fe03f70`) and tracked raw probe (SHA-256 `b17b30fed1e00c71e681df01dbd7d64c4998358a7be68e244d06826aded7a9ed`) bind the same locked 20-package roots, six project-owned target fixtures, two audited Docker images and a fresh owner-clean Docker/binfmt PASS on both MIPS targets. Architecture, endianness, o32, MIPS32r2 and soft-FPU match the pinned locks. AArch64 PASS in the hash-bound m1a-17 ledger plus these two new generic targets gives the specified A01 target ELF/shell/nested-exec check PASS on three generic targets. A10 matrix is NOT RUN; per-target proof does not meet its completeness/execution contract. The historical m1a-17 MIPS/MIPSEL SKIPs are unchanged. The host binfmt registration had separate explicit approval; MIPS `init`/`test` lifecycle is still not supported.

## MVP 1B m1b-19 matrix observation

`docs/evidence/m1b19-matrix.md` records the first complete three-profile matrix with one real, owned AArch64 IPK lifecycle PASS and two required static BLOCKED cases for unsupported MIPS/MIPSEL lifecycle. This is an A10 BLOCKED observation, not a three-target PASS. A separate locked wrong-architecture case returns FAIL/exit 1 and overrides BLOCKED; malformed inputs return 2 before Docker. No NDM/event or new MIPS lifecycle claim.

## MVP 1C m1c-26 native transport diagnostic (A14/A17 bounded PASS)

After separately approved native x86_64 NFQUEUE transport and host `xt_NFQUEUE` preparation, an owned AArch64 router observed queue-42 bind ACK, target IPC readiness and a real routed HTTP baseline without a rule. The selected target `/opt/sbin/iptables` is absent (exit 127), so no rule, queued packet, target verdict, counter increment, pcap, no-listener case or ACCEPT/DROP/restored ACCEPT was verified at that checkpoint. `docs/evidence/m1c26-ipc-backend-blocker.json` SHA-256 `c4f2af1804355c3b77304acd95ba4b613c5ba698a5fcd0e6aa4308ad1ac2b25f` is raw; `locks/m1c26-ipc-prototype.json` pins the diagnostic binaries/sources; `docs/evidence/m1c26-ipc-backend-rollback.json` SHA-256 `af520a6fcc41691edaf56588d89c1fe1f7875d51b9f14d2631879fef6e3cf59e` records safe removal of unused `xt_NFQUEUE` and clean owner resources. This was a historical BLOCKED result, superseded by the later bounded packet run below; direct target NFNETLINK remains unproven.

The subsequent approved native `iptables-legacy` backend has locked Debian source/package/extension/dependency/image identities in `locks/m1c26-native-iptables.json` SHA-256 `9e20ac7bf8a3cf3b8b2731d3e9e431bd5ac08f85ec341817f217f2b6b2ba4212`. Read-only owned-router table inventory was empty, while `iptable_filter` was absent from loaded modules yet available as a loadable host file; the approval at that time forbade other modules. No firewall command, rule, queued packet, counter or pcap was run at that checkpoint. `docs/evidence/m1c26-iptables-prerequisite-blocker.json` SHA-256 `18d681c2b02d74492e77752e07fe3acf8b2253baa33d76bbf998f50f15fb8313` records the historical BLOCKED prerequisite and safe unused `xt_NFQUEUE` rollback; later explicit module approval superseded that prerequisite.

Later, bounded `iptable_filter` preparation and fresh hash-checked `xt_NFQUEUE` loading enabled the exact owner-router rule. A native firewall helper with `NET_ADMIN` alone could not read the legacy filter table; a same-namespace raw-socket diagnostic failed without and succeeded with `NET_RAW`, consistent with the helper's permission gap. With only `NET_ADMIN`+`NET_RAW` granted to the owner-scoped helper, the live opt-in test passed: target 10 ACCEPT/7 DROP, adapter 17 matching verdicts, kernel queue sequence/rule counter 17, and bounded 17-record pcap (1420 bytes), with ACCEPT→DROP→restored ACCEPT routed HTTP and fail-closed no-listener 17→25 rule counter. Raw JSON and pcap are `docs/evidence/m1c26-packet-verdicts.json` and `docs/evidence/m1c26-packets.pcap`; exact rule removal, no owned Docker resources and ordered module rollback are recorded in `docs/evidence/m1c26-final-rollback.json`. Full suite 202 passed/29 opt-in skipped; the live opt-in test itself passed separately. This closes the frozen packet subtask under D-038/D-039 substitutions, not direct target NETLINK_NETFILTER, generic package compatibility or full MVP 1C.

## MVP 1B m1b-22 frozen three-target acceptance and capability observation

`docs/evidence/m1b22-acceptance.json` (SHA-256 `cf0dca2c0775fed8b055dd6454ecf4195957eb8dd9048d83751b10509d481841`) and `docs/evidence/m1b22-acceptance.md` bind tracked raw JUnit, complete matrix parent/child, per-target Docker capability probe and per-profile doctor results. On this shared Docker host the selected opt-in suite was 28 PASS/0 FAIL/0 SKIP; pre-ledger portable suite 187 PASS/0 FAIL/22 opt-in SKIP. A01's specified three generic target-exec check is PASS, not general MIPS package lifecycle. A02–A06/A10/A11 are BLOCKED as whole IDs for all three targets; AArch64 negative package reports remain FAIL as expected with PASS harness tests. Matrix has AArch64 child PASS, MIPSEL/MIPS static BLOCKED and aggregate BLOCKED. Synthetic host NDM subprocess proof does not establish target/device A11. Every target NFNETLINK socket call returned `Protocol not supported` while the same-namespace native control succeeded; no queue bind, packet, verdict, firewall or module mutation occurred. All project-owned probe containers were verified absent; doctor remained BLOCKED/4 on worker binfmt visibility. MVP 1B remains BLOCKED. See ADR-0014 for limits and source bindings.

## P0 gate summary

The P0 technical-risk gate is evidenced: AArch64 target execution is real, localhost TCP/UDP publish plus down/up persistence is real, and NFQUEUE is reproducibly BLOCKED rather than substituted. This does not make A01, A18, A19, A20, A21, A13, A14, or A17 complete beyond the exact statuses above, and it does not complete MVP 1A, 1B, 1C, or MVP 1.
